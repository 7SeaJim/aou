dist
node_modules

__pycache__/

# 编辑器配置,不是项目内容
.obsidian/

# 商务文件,不进公开仓库。
# **整个目录一起忽略,不是只挡那两个文件名** —— 原来写的是
# `分成协议*.md` / `账目表*.md`,现在里面正好只有这两个文件,所以看着没事。
# 但哪天往 合同/ 里丢一个 PDF 或者扫描件,它就不在规则里了,
# 而 `git add -A` 不会问你一句。这是个只会在出事那天才发现的写法。
合同/
分成协议*.md
账目表*.md

# 素材对比图,临时的
*-preview.png
