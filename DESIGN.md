# 哇鸥 · 去大坝整点饵块 · 策划稿 v0.2

> 状态:**P0 – P4 完成,P5「触达」做了一半,存档 v8。**
>
> 网页版见 `design.html`(`npm run dev` 后访问 `/design.html`)——
> 同一份内容的两个视图:这份便于 diff 和搜索,网页版便于读和给人看,
> 而且里面的哇鸥是直接 import `js/game/pixels.js` 画出来的,形象一改就跟着变。
> **改了一边记得同步另一边。**

## 一、一句话

在昆明滇池边,陪一只没跟着鸥群飞回西伯利亚的红嘴鸥,慢慢把一个小吃摊做起来。

---

## 二、设定

### 2.1 为什么是红嘴鸥 —— 这不是设定,这是昆明的真事

每年 11 月到次年 3 月,红嘴鸥从西伯利亚飞几千公里到昆明越冬,聚在翠湖和滇池海埂大坝。
1985 年它们第一次成群进城,昆明人喂海鸥从此成了一年一度的城市仪式。

**红嘴鸥的招牌就是红嘴和红脚** —— 和形象稿一模一样。所以哇鸥不需要额外解释自己是谁,
它就是每年冬天昆明人排队去喂的那种鸟。

这条设定顺手解决了三件事:

| 问题 | 设定给的答案 |
|---|---|
| 主角为什么在这儿 | 鸥群春天飞走了,它没走 |
| 为什么有客人 | 游客本来就是来喂海鸥的,顺便买点吃的 |
| 季节循环从哪来 | 冬天鸥群在(热闹),夏天鸥群走(冷清)——天然的养成节奏 |

### 2.2 哇鸥

- **名字**:来自叫声「哇——」。
- **身份**:留下来没走的那一只红嘴鸥。暂时居住在昆明。
- **性格**:贪吃、话密、怕冷、对递到面前的食物毫无抵抗力。
- **它在干什么**:在海埂大坝支了个小吃摊,卖给来喂海鸥的游客。
  ——被喂了这么多年,总得回请一次。

### 2.3 场景:滇池

主场景从「一个泛泛的海边码头」改成**滇池 · 海埂大坝**:

- **前景**:大坝步道 + 哇鸥的小吃摊 + 栏杆、缆桩、成堆的鸥粮袋
- **中景**:滇池水面、渔船、成群的红嘴鸥(冬季密、夏季稀)
- **远景**:**西山睡美人** —— 侧卧的女子轮廓,是滇池最好认的天际线;
  现在画面里那两座无名小岛应该换成它
- **天气**:昆明四季如春,雨季(6–10 月)雷阵雨说来就来,干季晴得发白。
  现在的 晴 / 雨 / 雾 三档保留,雾改成滇池早晨的水汽

> 已完成:`paintIsland()` 换成了 `paintXishan()` —— 睡美人存成一条
> 「(x, 高出水面多少)」的折线,中间线性插值,想让鼻子再挺一点就改一个数。
> 灯塔删掉了(滇池没有灯塔),换成大坝路灯;`SCENERY.stand` 重画成中式小吃摊。
> 飞行视角把山按 0.55 缩了一档 —— 不缩的话山脊会顶进食材的生成区,东西掉山上就看不清。

---

## 三、形象规格(已定稿)

| 项 | 值 |
|---|---|
| 飞行图 | 32×32,四帧(上→平→下→平),身体不动只换翅膀 |
| 图标 / 站姿 | 16×16,`px-icon--waou` |
| 源文件 | `tools/scenery.py` `WAOU` / `tools/icons.py` `ICONS['waou']` |

配色(在 `tools/pal.py` 里新增了两格):

| 字符 | 色值 | 用途 |
|---|---|---|
| `X` | `#e8384f` | 哇鸥红 —— 嘴、脚。角色的招牌色,别挪作他用 |
| `V` | `#dfe4e8` | 肚皮灰 |
| `w` | `#fffdf4` | 身体白(沿用浪花白) |
| `K` | `#4a3628` | 描边、眼睛、翅膀(沿用深褐,不用纯黑,和全站描边一致) |

**三个识别点,任何新动作、任何尺寸都不能改:**

1. **身体是圆的**(或椭圆)。不做写实的鸟脖子、不掐后颈 —— 圆是这个角色的底。
2. **豆豆眼**:一颗实心圆点 + 一点高光。
3. **三根睫毛**,外侧那根往外撇一格;**红嘴红脚**用哇鸥红。

> v0.3 改过一次:眼睛原本是「白」字形(黑块中间一道白横)。
> 放到待机界面的大特写上之后太凶,不萌,整个角色的气质就偏了 ——
> 换成豆豆眼。**所有尺寸一起换**(16px 图标 / 32px 飞行四帧 / 表演三帧 / 特写),
> 待机界面和游戏里长两个样是最要不得的。

后续要补的动作(按优先级):

1. `idle` 待机(轻微起伏,偶尔眨眼)—— 摊位界面常驻,放置玩法里是主视觉
2. `eat` 吃东西
3. `happy` 开心(收成好 / 升级)
4. `sleep` 打盹(离线回来时的画面)
5. `cold` 发抖(冬天 / 雨天)

---

## 四、内容替换

### 4.1 食材

已全部替换(旧的 薯条/汉堡/鱼/贝壳/海藻/蛋/面包 是通用西式套路):

| key | 名字 | 来源 | 备注 |
|---|---|---|---|
| `erkuai` | 饵块 | 觅食 | 昆明早点的地基 |
| `potato` | 洋芋 | 觅食 | 云南话就叫洋芋,不叫土豆 |
| `rice` | 米 | 觅食 | 米线、凉虾都从这来 |
| `douhua` | 豆花 | 觅食 | |
| `flower` | 鲜花 | 觅食 | 玫瑰,斗南来的 |
| `mushroom` | 菌子 | 稀有 | 雨季限定,见 4.2 的彩蛋 |
| `rusan` | 乳扇 | 稀有 | 严格说是大理的,但昆明满街在卖 |
| `chili` | 辣椒 | 常见 | |
| `sugar` | 红糖 | 常见 | |

### 4.2 料理

| 料理 | 材料 | 解锁 | 备注 |
|---|---|---|---|
| 烧饵块 | 饵块 ×2 + 辣椒 | Lv1 | 开局第一道,昆明人的早饭 |
| 洋芋粑粑 | 洋芋 ×3 | Lv1 | |
| 米凉虾 | 米 ×2 + 红糖 | Lv2 | 夏季卖得好,冬季卖不动 → 季节机制的第一个挂点 |
| 豆花米线 | 米 ×2 + 豆花 ×2 | Lv2 | |
| 小锅米线 | 米 ×3 + 辣椒 ×2 | Lv3 | |
| 鲜花饼 | 鲜花 ×3 + 饵块 | Lv3 | |
| 烤乳扇 | 乳扇 ×2 + 鲜花 | Lv4 | |
| 汽锅鸡 | 菌子 ×2 + …… | Lv5 | 压轴 |
| **见手青** | 菌子 ×3 | Lv4 | **高风险高回报**:小概率进入一段「看到小人」的特殊对话,当天收益翻倍或归零。做成整个游戏最有名的那个梗 |

> 折耳根做成分歧道具:客人有的加钱要,有的看到就走。

### 4.3 明信片(昆明地标)

已换成 10 张昆明地标,收集顺序从近到远。每张配了一句哇鸥的短评,界面上直接显示:

1. **海埂大坝** —— 起点,喂海鸥的地方
2. **西山龙门** —— 抬头就能看见的那座山
3. **翠湖** —— 城里的另一个鸥群据点
4. **金马碧鸡坊** —— 最好认的地标
5. **昆明老街** —— 文明街那一片
6. **云大会泽院** —— 银杏
7. **斗南花市** —— 鲜花饼的鲜花从这来
8. **篆新农贸市场** —— 食材图鉴的老家
9. **南屏街**
10. **石林** —— 远足,后期解锁

> 明信片是**主要的长线收集目标**,建议每张配一句哇鸥的短评,
> 比图片本身更值钱。

---

## 五、玩法:转向放置养成

### 5.1 当初的问题(已解决)

原来是「一局一局的飞行小游戏 + 手动做菜」,每日 5 次觅食。
关掉页面等于什么都不发生,进度 100% 由主动操作驱动。
这对一个「陪伴感」的小游戏是错的 —— 玩家想的是「今天哇鸥怎么样了」,
而不是「我要打满 5 局」。

### 5.2 目标形态

```
摊位自动出货 ──→ 离线时也在出 ──→ 回来收一波 ──→ 投进升级 ──→ 出得更快
     ▲                                                          │
     └──────────────────────────────────────────────────────────┘

飞行小游戏从「主玩法」降级为「加速器」:
想要稀有食材(菌子、乳扇)才去飞一趟,不是每天必须打卡
```

一句话:**主循环变成放置,飞行变成可选的爆发**。

### 5.3 目标形态下需要的系统

| 系统 | 说明 |
|---|---|
| **离线结算** | 回来时按离线时长补产出。设收益上限(建议 8 小时),避免挂机一周回来一次性通关 |
| **摊位产能** | 每种料理有「每分钟产出」,受灶台等级、招牌等级、天气、季节影响 |
| **升级树** | 灶(产速)、招牌(客单价)、货架(离线上限)、保温箱(离线衰减) |
| **伙计鸥** | 其他红嘴鸥来打工,每只带一条被动加成 + 一段对话。**养成线的主体** |
| **季节** | 冬天鸥群在:客流 ×1.5,伙计可招募;夏天:客流低,但雨季菌子多 |

### 5.4 表演:食材的被动来源

P2 做完之后暴露的问题:**摊位一直在吃食材,食材却只能手动囤。**
挂机回来常常是「摊子停了,因为没材料」——放置玩法的手感直接断掉。

补法是给哇鸥加一个被动产出:不出去觅食的时候,它在大坝上表演,路人看了会投喂。

| | |
|---|---|
| 节目单 | 8 个,由**等级 / 成就 / 明信片**三处分别解锁 |
| 节目越多 | 围观的人越多、投喂越勤(20s → 9.8s)、给的种类越杂(2 → 9 种) |
| 等级越高 | 一次给得越多(`1 + floor(level/4)`) |
| 天气 | 下雨大坝上没人,投喂减半 |

解锁条件**刻意分散在三处**:让「去过的地方多」这件事真的有回报,
而不只是图鉴上多一个格子。

**它故意补不满。** 全解锁大概能养住一个没升过级的格子;炉子一升、格子一开,
供给立刻跟不上,还是得出去飞。补满了的话飞行玩法就又没意义了 ——
这是整个三角的平衡点:

```
        ┌──── 表演:被动、慢,补不满 ────┐
        │                              ↓
   觅食一趟 ─────→ 背包 ─────→ 摊位出餐 ──→ 鸥币 ──→ 升级
   (主动、量大)      ↑          吃食材            ↑
                    └────────────────────────────┘
```

配套改了觅食的产出:`haulPerPickup = 1 + floor((level-1)/3)`,
Lv1 一次捡 1 个、Lv10 捡 4 个。摊位吃得快之后,再一颗一颗捡就跟不上。

顺带修了个老 bug:明信片掉落里写死着 `[...Array(7).keys()]`,
换成 10 张昆明地标之后**后三张永远掉不出来**。改成从 `POSTCARDS` 取。

### 5.5 P2 做完之后的三条记录

**① 摊位吃背包里的食材。** 这是当初没写死的一个选择,现在定了:摊位出一份就扣一份
材料,扣不动就停着。不这么做的话觅食和挂机是两件不相干的事,飞行玩法立刻失去意义。

**② 在线和离线共用 `produce()`。** 只有倍率不同(在线 1,离线走保温箱的折扣)。
分成两份实现的话,迟早在某次改数值时算出不一样的结果。

**③ 挖出一个真 bug:`tickStalls` 把任意时间差都当成在线。**
笔记本合盖三小时、标签页被浏览器冻结再唤醒 —— 页面还开着,但下一次 tick
会一口气看到几个小时的时间差,按在线全额结算,离线上限和折扣全绕过去。
结果是**挂着标签页睡一觉比老老实实关掉页面赚得多**,方向完全反了。
现在超过一分钟就转交 `settleOffline()`。

另外两个防挂机的细节:材料断供时把计时压回一个周期以内(不压的话补上材料会
一次性补发几百份);多个格子抢同一批食材时按格子顺序先到先得(简单、可预期,
玩家能靠调整顺序决定谁优先)。

### 5.6 数值骨架(实际落地的)

- 出餐间隔:`24 秒 ÷ 炉子系数`,炉子 1→10 级是 ×1.00→×3.52
- 每份收入:`菜价 × 招牌系数 × 天气系数`,招牌 1→10 级是 ×1.00→×2.62
- 天气:晴 1.0 / 雨 0.8(大坝上没人)/ 雾 0.9
- 离线上限:货架 1→8 级是 2→9 小时
- 离线折扣:保温箱 1→8 级是 40%→75%
- 升级成本:`base × 1.5^(等级-1)`,四条线的 base 分别是 60 / 90 / 150 / 200
- 格子:Lv1 一格,Lv3 两格,Lv6 三格

原稿写的是 `1.15^等级`,实际用了 `1.5^(等级-1)` —— 1.15 太平,
十级下来总成本还不够挂机一小时的收入,升级就没有选择感了。

### 5.7 落到现有代码上动了什么

| 文件 | 改动 |
|---|---|
| `js/state.js` | 加 `lastSeen`(时间戳)、`stalls`、`upgrades`、`crew`;**`SAVE_VERSION` +1 并补一条迁移** |
| `js/game/rules.js` | 加 `settleOffline(state, now)`,和现有 `settleFlight` 一样返回事件数组 |
| `js/storage/savecode.js` | 字段变多,注意存档码长度(现在约 350 字符) |
| `js/ui.js` | 主界面从「码头」改成「摊位」:显示待收、产能、升级按钮 |
| `js/game/flight.js` | 保留。触发条件从「每日 5 次」改成消耗体力 / 有目标地去找稀有食材 |
| `js/data.js` | 食材、料理、明信片、对话全部重写 |

⚠️ **离线结算用的是客户端时钟,改系统时间就能刷。** 单机无所谓 ——
这一点和现在 `refreshDaily()` 的处境一样,`js/game/rules.js` 里已经写了同样的注释。
将来若要做排行榜,时间必须由服务端判定。

---

## 五之二、待机界面

进游戏前先看见一张图:**哇鸥站在长虫山的观景台上眺望昆明。**

**是侧视,不是水平示意图。** 深度顺序:城市 → 石栏杆 → 哇鸥 → 草木。
石栏杆走透视线(远端高而细、近端低而粗),哇鸥站在近端、在栏杆**前面**,
左下角有一丛压在镜头前的枝叶 —— 像摄影师从灌木后面偷拍到的。

从远到近:天(色带,白天/傍晚/夜晚每次随机)、远山、
城市(**万达双塔**明显高出一截,是一眼认出昆明的那个点)、
栏杆、哇鸥(贴着右缘,只看得见左半边,朝左凝视)、草木。

几条做的时候才发现的:

- **中景不能空着。** 城市脚下到崖顶那一段一开始整块没画,直接漏出了
  `.px-screen` 的 CSS 底色,一片浅青,看着像水其实是个洞。得先铺满再往上画。
- **标题和按钮不能都摆正中**,那样正好压住双塔 —— 整张图的地标。
  改成标题放天上、按钮放城市下面那条带上。
- **特写就得够大。** 第一版哇鸥画了 60×84,摆在 440×310 的画面右下角只有巴掌大,
  不像特写像贴纸。放大到 100×132 才立得住。
- **草木不能缩在角落。** 第一版画成一小丛,读着是「草地上有点草」,
  不是「镜头前有枝叶」。要大到压过栏杆、尖子探进画面中段才有那个意思。
  叶子也得用真正的叶形(两头尖中间宽),圆点堆出来是一团黑饼。
- 游戏本体在待机界面背后已经跑起来了,点下去是立刻开始;
  离线结算的吐司先攒着,等玩家真进来了再弹,不然它会在标题画面背后自己弹完。

调试用 `?time=night` 钉住时段。

## 五之三、小屋

堤岸边的草棚,**全游戏唯一的近景**。别处都是远远看着哇鸥,只有这里能看清它的脸 ——
所以棚里除了必要的结构什么都不放,视线全给它。

**全游戏只有一个时钟。** 大坝的天光和小屋开不开门都从 `hourSlot` / `dayPhase` 读 ——
一开始小屋自己一套时段、大坝根本没有时间概念,两边对不上,这是个必须先修的地基问题。

| 时段 | 哇鸥在哪 |
|---|---|
| 11:30 – 13:30 | 小屋(醒着) |
| 19:00 – 22:30 | 小屋(醒着) |
| 22:30 – 05:30 | 小屋(睡着) |
| 其余 | 大坝上 |

「太晚了只能看它睡觉」这条是刻意的 —— 有个见不到的时间段,见到的时候才算数。

同步带来的两条连锁反应,都得跟着做,不然就是「看起来同步了」:

- **它不在大坝时,大坝画面里没有它,表演也不产食材。** 离线结算按五分钟一格
  采样算出那段时间有多少在大坝上(`damMsIn`)。睡着还有人投喂就说不通了。
- **入口做进背景,不是又加一个按钮。** 大坝上画了个草棚,点它进小屋;
  画和点共用 `SHACK_HIT` 一份坐标。

`hourSlot` 和 `dayPhase` **刻意分开**:前者管「哇鸥在哪」,后者管「画面什么颜色」——
它 11:30 回屋,但天不会在 11:30 变暗。

| 互动 | 说明 |
|---|---|
| **占卜** | 天气 + 贝壳花纹 → 八分之一。**八种是硬上限**:再多就成了随机文案,少了又转不出新鲜感。一天一次,让它成为一个仪式而不是一个按钮 |
| **海鸥四子棋** | 黑白石头是从大坝底下捡的。AI 会赢会堵、会避开送分,但**不做搜索树** —— 它是只海鸥,不是引擎 |
| **请它喝一杯** | 普洱茶 / 咖啡,每天上线随机给一杯。两样都是云南的(保山咖啡),不是随便挑的饮料 |

几条做的时候的判断:

- **占卜暂时只有文案,没有数值加成。** 给它挂个「今日产出 ×1.3」很容易,
  但那会让占卜变成每日必点的最优解,坏了「仪式」的性质。先留成纯风味,
  真要挂效果,得连着「不好的卦要不要有负面」一起想。
- **哇鸥在屋里尽量圆。** 第一版把眼睛按「大图就该多细节」画小了,出来是个蛋。
  比例得照 32px 那版等比放大 —— **眼睛占身宽约四分之一**,这是萌的关键。
- **腿是画的,不是帧图。** 走路时腿的角度一直在变,做成精灵图要画一大堆,
  而且改一次走路速度就得重画。

## 五之四、养成

**季节按真实月份走。** 红嘴鸥 11 月到次年 3 月在昆明越冬是真事,不该变成一个
游戏内的抽象计时器。代价是夏天冷清好几个月 —— 这是故意的:冬天鸥群回来时
才有那个落差,一年到头一样热闹就没有季节了。

夏天不是纯粹的惩罚:雨季稀有食材翻倍(菌子),所以夏天该屯货、冬天该开摊。

**伙计鸥**六只,一只管一件事、不叠加同类效果,玩家一眼看得出该先招谁。
两道门槛都是从设定里长出来的,不是硬塞的数值墙:

- **好感度** —— 得先和哇鸥处熟了,它才肯把亲戚介绍给你
- **只有冬天招得到** —— 鸥群不在昆明的时候,没人可招

**食材图鉴**记的是累计见过多少,花掉了不减。所有加食材的路径都走 `gain()`,
拿背包反推会越玩越少。老档升 v6 时按背包开个底 —— 手上有的显然见过,
不开底的话老玩家升上来是一本空图鉴,看着像坏了。

## 五之五、长线(P4)

P3 做完之后暴露的问题:**所有系统都各自封闭,玩通一条线不会让另一条线变好玩。**
成就页更是空转 —— 七条全是飞行玩法的遗物,放置、表演、小屋、伙计一条都没有,
而且达成了只弹一个窗,什么都不给。

P4 三件事其实是**一件事的三段**:成就产出瓶盖,瓶盖买装扮,装扮的解锁条件
又挂回明信片 / 伙计 / 好感度。随机事件负责把这条线撒到日常里。

### 5.5.1 成就重做

七条扩到 28 条,按玩法分五组(觅食 / 摊子 / 大坝 / 小屋 / 见闻),
组内分三档。分组不是为了好看:**平铺一长条时玩家看不出「还有哪块没碰过」,
分组之后空着的那一栏本身就是引导。**

判定要靠计数器,不能拿背包和图鉴反推 —— 花掉的、送出去的都该算数。
所以存档里加了 `stats`:出去觅食几次、被投喂几次、摊子出了几份、
请喝了几杯、赢了几局棋、撞上几件事、累计挂机多久。

老档升 v7 时这些**只能从 0 起**。四子棋战绩和今日占卜本来就存着,
那两处开个底;其余补不出历史,与其瞎猜不如诚实归零 ——
老玩家会慢一点,但不会凭空多算。

还修了一个一直都在的漏子:成就原来只在「做菜 / 交订单 / 离线结算」时判,
**纯挂机涨上来的等级、钱、投喂次数一直不算数**。现在在线 tick 每秒也判一遍。
代价是老档进来会一口气达成一串,所以吐司超过三条就并成一条。

### 5.5.2 装扮

五件,两个槽位(头 / 脖子),一个槽只戴一件。

**用瓶盖买,不用鸥币。** 两条线抢一个钱包的话,玩家买了帽子就升不了炉子,
装扮会变成「明知道该忍住的浪费」。瓶盖只从成就来,于是「打扮」的进度条
就是成就本身。全部达成共 120 根,五件总价 68 根 —— 留了余量,
不至于逼玩家把成就刷满才能买齐。

解锁条件全挂在别的玩法上:花环要去过斗南花市、头巾要招够三只伙计、
铜铃要好感度 30。**逼着玩家把三条线都碰一遍。**

素材在 `tools/wear.py`,每件两套图:

- `big` 给小屋近景那只 124×99,按 46 宽画完再 ×2 —— 直接画 92 宽一行要数
  九十多个格子,数错一个就歪;放大两倍之后描边正好 2px,和 hut_waou 一致
- `small` 给大坝上那只 16×16,十几个格子直接画

**一套图缩放到另一个尺度不是糊就是一堆大方块**,所以必须两套。
锚点按「头顶那一行」对齐而不是精灵图底边 —— 大坝那三帧高度不一样,
鞠躬那帧头低了 3 格,按底边对齐帽子会浮在脑袋上面。

飞行玩法里的 32×32 和待机界面的特写**暂时不戴** —— 那是第三、第四套图。

### 5.5.3 大坝随机事件

18 条,哇鸥站在大坝上的时候平均九分钟撞上一件。和表演共用同一条判据
(`damMsIn`)—— 它人在小屋里,坝上发生什么都跟它没关系。

三条约束,让事件是「调味」不是「主收入」:

1. 一次的量都不大,抵不上一趟觅食;量跟等级走,但只跟到能看出差别为止
2. **有好有坏。** 全是好事的话,事件就只是个慢速的自动奖励 ——
   小孩追鸥群会把表演进度清零,来巡的人会把摊位计时清零
3. 离线照常发生,但打折并且**一次最多补 6 件**。不封顶的话出门一天
   回来是三十条日志,一条都不会有人看;而且坏事攒一堆一起结算,
   玩家只会觉得被坑了

结算顺序是**事件 → 表演 → 摊位**,在线离线一致。反过来的话,
事件清掉的那些计时是「已经产出过一轮之后」才清的,等于凭空多给。

日志存的是**事件当时的原话,不是 id**:事件表以后会改,改完拿 id 反查
会让老日志对不上号。日志是「发生过什么」的记录,不是索引。只留最近 20 条 ——
存档码是要玩家复制的,日志无限长会把它撑成一大坨。

## 五之六、触达(P5)

前面五期都在往里加东西。P5 问的是另一个问题:**一个人第一次点开这个链接,
到他明白这游戏在干嘛,中间会被什么挡住。**

之所以现在做,是因为这两件事:比赛评委大概只会玩三分钟;朋友的号是小红书,
链接发出去几乎全是手机点进来的。这两条决定了「码头纵深画得多好」暂时排在后面。

四件已经做完的:

### 5.6.1 字体:588 KB → 45 KB

完整包 31345 个码位,游戏实际用到一千四百出头。剩下三万个字形是纯过路费,
而且这笔钱是**首屏**付的。裁完 45 KB,`dist` 里的字体从 2.07 MB 降到 45 KB
(里头本来还压着一份 1.47 MB 的 `.woff`,浏览器根本不会去取)。

漏字不会报错,**只会无声无息掉回系统字体** —— 一句话里几个字忽然变样,
这种事没人会在提交前发现。所以 `npm run build` 先跑一遍 `font.py --check`。
检查连注释里的字一起收:与其写个半吊子的注释剥离器(剥错了就是静默漏字),
不如多收几 KB 换一个只会误报、不会漏报的检查。

### 5.6.2 手机:真正的问题不是画面

**先前我判断错了一次,记下来。** 我以为手机上画面会被压成非整数倍缩放而糊掉,
量完发现关键是**设备像素而不是 CSS 像素**:

| | CSS 宽 | 设备像素 | 对 880 位图 |
|---|---|---|---|
| iPhone 15(DPR 3) | 354 | 1062 | 1.21× 上采样 |
| 中端安卓(DPR 2) | 324 | 648 | 0.74× 轻微下采样 |
| 桌面窗口拉到 390 宽(DPR 1) | 354 | 354 | 0.40×,明显 |

上采样不掉像素,真机上是干净的;0.40 那行是把桌面窗口拉窄,不是手机。
所以缩放策略没动。

真正**会让手机玩不了**的是另一件事:飞行玩法要在画布上按住不放
(右半边按住 = 平飞),而默认情况下浏览器把手指在画面上的移动当成滚页面,
直接发一个 `pointercancel`,那一下平飞就断了。一行 `touch-action: none`。

窄屏只收了排版,页签从三行换行改成一条横着滑 —— 九个页签在 360 宽上要占
三行,最后一行还孤零零一个「存档」。

### 5.6.3 音效:一个音频文件都不带

十三个音全部现场合成。理由和像素画同一条:美术是几十 KB 的字符网格换来的,
再挂两百 KB 的 mp3 就本末倒置了;芯片音本来就该是振荡器出来的。

**AudioContext 只能在用户手势之后建** —— 页面一加载就 new,拿到的是
suspended,之后再怎么 play 都没声。所以是懒建的。

静音开关存 localStorage **不进存档**:它是这台设备上的偏好,不是玩家的进度。
存档码是要复制给别人、换设备用的。

### 5.6.4 开场引导

四步,目标很具体:**让第一次打开的人在半分钟里看懂三条线是一个圈** ——
出去觅食囤材料 → 摊子自动吃材料出餐 → 不出去时在坝上表演被投喂补材料。
这三条各自都不难懂,难的是它们咬在一起;九个页签一次铺开,没人会自己拼出来。

**每一步靠读状态判定,不靠「玩家点了哪个按钮」。** 挂在按钮上的引导,
一旦玩家用别的路径达成(比如从大坝页直接跳摊子),就会卡在原地
等一个永远不会来的点击。

走完送 5 根瓶盖 —— 正好一顶斗笠,顺手把「装扮」这条线也指出来。

老档升 v8 时一律直接标成「走完」:**能升级上来的存档,人早就会玩了**,
给一个玩了两周的人弹「点一下出发觅食」是冒犯。

## 五之七、传播(P6)

前面所有东西都只在游戏里有意义。P6 问的是:**玩家玩完之后,手上有什么可以发出去的。**

这条对这个项目格外重要 —— 合作方是小红书的 IP 运营方,游戏如果不能给账号供稿,
两边就是各干各的。

### 5.7.1 今日运势卡片

占卜本来就是「每天一次、有结果、有文本」,这个形状天生适合成图。
游戏里其余的东西(数值、进度)截图出去别人看不懂,**一张签文别人看得懂**。

尺寸 360×480,整数放大 3 倍 → 1080×1440,3:4 是小红书的竖图比例。
沿用低分辨率作画那一套:先在 360×480 上画完再整块放大,不在大画布上直接画,
否则文字和像素图会一个软一个硬。文字只用 12 / 24 两个字号 ——
字体本体是 12px 设计的,别的字号都在拉伸。

三个必须记住的坑:

- **卡片是给没玩过的人看的。** 所以正文要能单独成立,不能写
  「今天摊位收益 +10%」这种只有玩家读得懂的话。落款里游戏名单独占一行居中,
  缩在角落和日期挤成一条的话,别人根本注意不到这是个游戏。
- **画完转成 data URL 塞进 `<img>`,不直接摆一块 canvas。**
  手机上长按 `<img>` 才有「保存图片」,长按 canvas 没有;而在国内的浏览器和
  App 内置浏览器里,长按保存比 `<a download>` 靠谱得多。
- **必须等 `document.fonts.ready`。** canvas 画字是一次性的:字体还没到就画,
  会拿系统字体先落下去,而且不会像 DOM 那样等字体到了自己回流重排。

顺带把 todo 里「占卜要加画面和详细文本」做了:八种签各加了 `long`(卡片正文)
和 `yi/ji`(宜 / 忌)。**签不带任何数值效果** —— 一个「大凶」真去砍掉当天的产出,
玩家只会学会「凶了就今天别上线」,一个每日一次的仪式不该变成开不开游戏的理由。
所以宜忌写成建议:指向玩法里本来就有的选择,读起来像有用,实际不动数值。

八种贝壳花纹是在同一个壳上加线加点画出来的,不做八张精灵图 ——
「一道纹 / 两道纹 / 三道纹」本来就只差几根线。

### 5.7.2 卡片的图先搭骨架,不定稿

图这一层**明确是暂定的**,所以先不追求画得多好,先把「能换」这件事做出来:
一张卡 = 背景 × 版式 × 天光,目前 4 × 2 × 3 = 24 种组合,
背景和版式各是一张表(`CARD_SCENES` / `CARD_STYLES`),加一个就是加一行。

配套一个 `/card-preview.html`,一页看全 24 种。**这一页才是重点** ——
以后改图是要反复看的,每改一次进游戏转一卦再截图,三轮就烦了。

背景里一律不写死颜色,用 `scene.js` 的 `pal()`。这样天气和时段是免费跟上的,
而且卡片和游戏画面不会漂成两个色调 —— 各调一份的话迟早漂。

## 五之八、数值体检(2026-08-31)

`tools/sim.mjs` 把 rules.js 按天空跑,不开浏览器。三种玩家画像 × 30 天,
从冬天起跑,另外单跑一次夏天起跑。**它模拟规则,不模拟飞行** ——
一局觅食吃到多少是代入的假设(写在 `RUN` 里),因为飞行需要 canvas 和一双真手。

跑出来的结论,按严重程度排:

### 1. 鸥币在第 2–8 天就失去意义 ⚠️ 最严重

整条升级线**一共只要 22496 鸥币**(炉子/招牌各 10 级、货架/保温箱各 8 级)。

| 画像 | 买满升级线 | 第 30 天鸥币 |
|---|---|---|
| 纯挂机(一天开一次 2 分钟) | 第 8 天 | 213 万 |
| 休闲(一天两次 6 分钟) | 第 4 天 | 665 万 |
| 活跃(一天四次 15 分钟) | 第 2 天 | 1263 万 |

买满之后**鸥币再没有任何去处**。一个放置游戏的主循环是「赚 → 买 → 赚更快」,
这条链在第一周就断了,后面二十多天的收入等于流水账。

### 2. 稀有材料是真瓶颈,普通材料在爆仓

休闲画像第 30 天:**鲜花 1、菌子 106、乳扇 440,而豆花 37025。**

背包总量看着有十几万,像是不缺;实际缺的永远是那两三样。而摊子只要摆了
用得上它们的菜,就会一直干烧 —— 三种画像的「出现过断料的天数」都是 29–30/30。

### 3. 背包会溢出存档上限

`normalize()` 给每样材料的上限是 9999。休闲画像**第 12 天**就有单样超了,
活跃画像第 7 天。**在内存里不拦,存盘再读才削** —— 玩家会看见数字凭空掉下来。

### 4. 夏天不是「冷清」,是「锁死」

夏天起跑的休闲画像出餐 975 份/天,冬天 1274 —— 只差两成,谈不上玩不下去。
真正的问题是:**夏天招不到伙计**(设定如此),而「蓝花头巾」这件装扮的
解锁条件是「招到 3 只伙计」。于是夏天入坑的玩家:

- 伙计 0/6,要等到 11 月
- 装扮 4/5,那一件跟着一起锁死

一个八月下载游戏的人,有两个系统要等三个月才开门。

### 5. 等级和收集线倒是没问题

升到 10 级:活跃第 2 天 / 休闲第 5 天 / 纯挂机第 30 天 —— 跨度合理。
成就 23–26/28,瓶盖 87–110/120,五件装扮总价 68 根 —— **瓶盖这条线是够的**,
纯挂机也能在第 20 天买齐。

### 怎么修 —— 已做(存档 v9)

三个方向里选了后两个,一起上。

**篆新市场(`MARKET`)。** 4 级开门,收摊的阿姨每天顺路来坝上摆一小摊。
拿鸥币换材料,价格按稀有度拉开(饵块 8,菌子 180)。

**每天限量是整个设计的关键**,不是随手加的平衡旋钮:不限量的话稀有材料
就变成纯粹用钱买,觅食和表演立刻都不用玩了 —— 那是拿一个洞去补另一个洞。
限量之后市场是「补缺口」,不是「代替玩法」。设定上她也只是顺路摆一小摊,
东西本来就那么点。

**成本曲线整体拉陡。** 底价不动(第一级还是 60 / 90 / 150 / 200),
公比从 1.5 提到 **2.5**,四条线上限各 +2。**只提公比不提底价**是有意的 ——
一刀切地把成本乘个 20 倍,受伤最重的是收入最低的那档挂机玩家,
他们连第一级都买不起;拉陡则是前期照旧、后期变贵。

顺带把两条涨得太凶的曲线放缓:觅食每 3 级 +1 改成每 5 级(15 级时一趟顶
五趟太夸张),表演每 4 级 +1 改成每 6 级(表演是完全被动的,
它涨得比觅食快的话主动玩法就没意义了)。基准出餐间隔 24 秒 → 45 秒。

还修了问题 3:`gain()` 现在就地封顶到 9999,和 `normalize()` 一致。
以前是内存里不拦、存盘再读才削,玩家会看见数字凭空掉一大截。

### 改完之后再跑一遍

| | 买满升级线(原来) | 买满升级线(现在) |
|---|---|---|
| 活跃 | 第 2 天 | **第 17 天** |
| 休闲 | 第 4 天 | **第 27 天** |
| 纯挂机 | 第 8 天 | 30 天没买满(7/44 级) |

整条线从 22496 鸥币涨到 327 万。

**还没解决的:后期依然有富余。** 休闲玩家第 27 天买满之后,剩下三天又攒了
一百多万没处花。这是「有限的升级树」的固有结果 —— 树总有买完的一天。
真要根治得上第一个方向(给鸥币一个无底洞),那是下一轮的事。
现在至少不是第四天就见底了。

**纯挂机那档依然吃力**(30 天 9 级、升级线 7/44)。那是「一天开一次两分钟、
只飞一趟」的极端下限,留着当地板参考。真要照顾这档,得让离线收益
自己也能推进升级,而不是靠上线时手动觅食 —— 也是下一轮的事。

## 六、分期路线

| 期 | 内容 | 状态 |
|---|---|---|
| **P0** | 像素画基建、UI 组件库、哇鸥形象 | ✅ 已完成 |
| **P1 · 换皮** | 滇池场景、云南食材与料理、昆明明信片、对话重写 | ✅ 已完成(存档 v2) |
| **P2 · 放置内核** | 离线结算 + 摊位产能 + 升级树 | ✅ 已完成(存档 v3) |
| **P3 · 养成** | 表演玩法、待机界面、小屋、伙计鸥、季节循环、食材图鉴 | ✅ 已完成(存档 v6) |
| **P4 · 长线** | 成就重做、装扮、大坝随机事件 | ✅ 已完成(存档 v7) |
| **P5 · 触达** | 字体子集、手机可玩、音效、开场引导 | ✅ 已完成(存档 v8) |
| **P6 · 传播** | 今日运势卡片、其余待定 | 🟡 卡片已完成 |

**P1 做完了,但当初那句「不碰存档结构」是错的。** 食材换名字就是在改存档:
`backpack` 的键从 `fries/burger/…` 变成 `erkuai/potato/…`,老档的背包会整个对不上。
实际做法是走了一次 `SAVE_VERSION 1 → 2` 的迁移,把旧食材按最近的对应关系换过来
(炸薯条→洋芋、汉堡和面包→饵块,两个旧键并进一个新键所以要累加),
明信片和订单清空重来(旧 id 对不上新地标,旧订单的 need 里是已经不存在的食材)。

顺带挖出一个真 bug:`migrations[0]` 用的是**当前**的 `FOOD_KEYS` 常量,
而它现在指向新键 —— v0 老档的背包会在第一步就被清零,第二步再也捞不回来。
**迁移函数只能依赖写死的、当时的字段表**,不能引用会随版本变化的常量。已修。

---

## 六之一·五、出摊(存档 v11)

主动做菜以前是摊子页最底下一列「制作」按钮,和「今日订单」并存 ——
**两件事其实是同一件事**:有人要一道菜,你给他。现在合成一个。

### 参照的是老爹快餐店

布局照着 Papa's 那一路,**压成一屏**(它是分工位切页签的,我们不切):

    上   订单        —— 每张卡片:要什么 + 还能等多久 + 「给」
    中   砧板/煎盘/灶台
    左下 烤箱        —— 它慢,是「放着不管」那一类,和要盯着的三件分开摆,
                       混在一起玩家的眼睛不知道该看哪儿
    右   手上在做的 + 出餐台 + 能开的菜

### 一道菜怎么做

一步 = **把某样食材拖到某件厨具上,再在火候窗口里点一下端下来**。

步骤照着真做法写,不是随便凑的节奏:烧饵块先烤饵块再切辣椒配,
见手青得下锅炒两遍(不炒透会看见小人,这个梗得留着)。
**玩家第一次做完一道菜,应该顺带知道了这道菜是怎么来的。**

### 火候的阈值:一道下限,一道上限

第一版的窗口是**时长的一个比例**,于是有两个连在一起的毛病:

* 短的那几步(切辣椒 1.5 秒)算下来「刚好」只有 0.6 秒。
  **同时开三四样的时候根本够不着** —— 手指还在拖上一样,这一锅就糊了。
* 火力提高会把时长按 1/power 压短,窗口跟着按比例缩。满级之后最短的一步
  只有 0.7 秒、窗口 0.6 秒 —— **玩家花钱买到的是「更难点中」**。
  一个休闲玩法里,升级该让人更轻松,不是更手忙脚乱。

放宽之后又露出了第三个,而且是同一个根:比例这件事**两头都会失控**。
长的那几步(蒸汽锅鸡 16 秒)算下来「刚好」有二十多秒,等于根本不用看火。
于是有了一条反过来的规律:**越贵越慢的菜,火候反而越不用管** ——
玩家一路解锁上去,手上功夫是越来越少的。

所以现在两头都夹住:

    MIN_STEP_MS  2400   一步最短多久,升级只能快到这儿
    GOOD_MIN_MS  1100   「刚好」那一段最短多久
    GOOD_MAX_MS  2600   「刚好」那一段最长多久

到了 MIN_STEP_MS 之后,再升级换来的是**格子数**(能同时开几样),
而不是继续压缩反应时间 —— 那才是「产能变大」该有的样子。

### 收紧火候,延长总体时间

这两件事是一起做的,而且方向相反才对:

* **步骤时长整体 +50%**(烤饵块 3.5→5 秒、进烤箱 11→16 秒、上汽蒸 16→24 秒)
* **窗口比例整体收紧**(砧板 0.60→0.30、煎盘 0.38→0.16、灶台 0.48→0.20、
  烤箱 0.80→0.34),再加上面那道上限

长的是**你能走开多久**,紧的是**回来那一下要多准**。
反过来做的话(步骤短、窗口宽)就成了一个不停催你、又怎么点都对的玩法。

改完「刚好」那一段落在 1296~2600ms 之间(原来是 1800~23040ms),
可以迟到 0.58~1.16 秒。验收标准照旧:三样同时开、每样晚 0.4 秒才腾出手去端,
三样都还是「刚好」;晚 1.2 秒则一律糊 —— 原来烤箱那一步晚七秒都不糊。

游客的耐心跟着从 42 秒放到 50 秒。不放的话汽锅鸡三步 33 秒,
现点现做必然超时 —— **拉长步骤就得同时看一遍所有以秒计的邻居。**

**判定和画条子必须共用一个函数**(`goodBand`)。两边各算一套的话,
玩家会遇到「明明停在绿带子里却判了生的」—— 那是最让人下头的一种不公平。
自动化里专门断言了这一条:所有菜 × 三个等级,绿带两端刚好是 good 的边界。

### 火候:过头和不到家一样糟

这是整个玩法唯一的手上功夫。进度条走到 1 是「熟了」,
`window` 是「刚好」那一段占的比例,贴着 1 结束;跨过 1 一点还来得及,
再拖就糊。

三档而不是连续打分:连续分数玩家读不出来自己差在哪,
**三档是「生了 / 好了 / 焦了」,一眼知道下次该早点还是晚点。**

四件厨具的窗口宽度不一样,这就是它们的性格:

| | 窗口 | 性格 |
|---|---|---|
| 砧板 | 0.55 | 快、不怕过火,新手最先学会的一步 |
| 灶台 | 0.34 | 中等 |
| 煎盘 | 0.22 | 最考手速 |
| 烤箱 | 0.75 | 慢,但窗口特别宽 —— **「提前做」靠的就是它** |

### 拖拽走 pointer 事件

不用 HTML5 drag:它在移动端基本不能用,而这游戏一半以上的人在手机上。
pointer 事件桌面和触屏是同一套代码。可拖的牌上必须写 `touch-action: none`,
不写的话手机上一拖就变成滚页面。

**厨房是 DOM 不是画布。** 核心动作是拖,而拖拽、命中判定、拖影这些
浏览器本来就有,没必要在 440×310 的画布上重造一遍 ——
何况手机上每个厨具不到一个指头宽。画布画它该画的:湖、柜台、排队的人、
趴在柜台后的折耳根。

### 厨具升级:和摊位那四条分开

摊位那四条(炉子/招牌/货架/保温箱)改的是**你不在的时候赚多少**;
厨具这四条改的是**你在的时候能多快**。两笔钱都从一个钱包出,
但玩家心里分得清:一条躺着赚,一条站着赚。

火力提高会让时间按 1/power 缩短,**窗口按比例跟着缩** —— 更快,但不更容易。

餐盘是纯外观 + 一点点加价。**加价必须小**,不然它就从「我喜欢这个花纹」
变成「我不得不买这个花纹」。

### 折耳根站柜台

做好的菜她会端给对的客人,但她是猫,慢(七秒一盘)。
**你自己动手总比她快 —— 她兜底,不抢活。**

### 案上摆的是真东西,不是四个工位

第一版的厨具是**四个一模一样的木框子**,框里几个方格,食材拖进格子里。
功能全对,但看着像流水线的工位 —— 玩家在操作一个表格,不是在做饭。

现在四件家什是**画在画布上的东西**,各是各的形状、各占案上一块地方:

    砧板  平摆着,左边一截手柄,板上横着刀
    煎盘  圆盘 + 伸出去的长柄
    灶台  铁皮灶身,炉门里见火,菜摆在灶眼上
    烤箱  嵌在案子正面的方箱,火从玻璃窗透出来

**画归画布,点归 DOM。** 判定框是透明的 `<div>`,盖在画好的东西上;
两边共用 `service.js` 里 `STATIONS` 那一张坐标表 —— 画一处、点一处地
各写一份的话,挪个位置就会「看着在这儿、点不到」。

不干脆全画在画布上,是因为核心动作是拖:拖拽、命中判定、拖影这些浏览器
本来就有,在 440×310 的画布上重造一遍不划算,手机上尤其。

火候条也搬到了画布上,**贴着东西本身画**:眼睛不用在「东西在哪」和
「烧到几分」两个地方来回跑。到「刚好」那一档时判定框显一道金框 ——
整屏只有这一个信号该这么响。

配套改了两处比例:

* 出摊这一场的分层和大坝那一场**故意不一样**。大坝看的是风景,这儿看的是
  做菜。原来天和湖占掉一半屏幕,案子挤在最底下一条,做菜的地方还没有
  空甲板大。现在外面压到 45%,案子那头拿走 55%。
* 案子正面那一片占了三成屏幕,原来是一整片空木板。**摊子底下本来就堆着东西** ——
  摞起来的碗、装米线的竹筐、几袋米、一只煤气罐、一摞蒸笼。空着的三成,
  和「贴图没加载出来」是一个观感。

### 什么时候能出摊:一条规则

**哇鸥在大坝上,摊子就开着。** 也就是 05:30–11:30 和 13:30–19:00 ——
它回草棚歇脚(晌午 / 晚上 / 深夜)的时候摊子跟着收。

原来还多一条「天亮着」,于是 17:30 天一擦黑摊子就收。两个问题:
海埂大坝人最多的恰恰是傍晚;而下班后才打开游戏的人,进来发现摊子已经关了。
并成一条之后玩家少记一件事 —— **它人在哪儿,摊子就在哪儿**,
大坝画面上摊子有没有人一眼就能看出来,不用背时刻表。

时段字串写在 `SERVICE.span`,是 `serviceOpen()` 的人话版本,两处要一起改。

打烊的时候进出摊页,画面正中压一块牌子说明什么时候开,并且**后厨照样进得去** ——
摊子没开正好是升厨具的时候,一起关掉等于罚玩家来早了。

### 存档

v9 → v10:加 `stock`,删 `orders`。
v10 → v11:加 `kitchen`(厨具等级 + 餐盘),`stock` 从「几份」变成「几份 + 品质」。
老档那几份按「刚好」给 —— 他们做的时候还没有火候这回事,不该判他不合格。

### 五个踩过的坑

**一、`mutate` 会同步触发重绘,所以「改完状态再通知」这条要严格照办。**
装盘那一步原来是先 `mutate(finishDish)` 再把这道菜从 `dishes` 里摘掉 ——
中间那一瞬间 `d.step` 已经越过最后一步,重绘去读第 step 步拿到 `undefined`,
整个厨房炸掉。摘在前、写在后就好了。

**二、headless chromium 加 `--virtual-time-budget` 不给动画帧。**
`requestAnimationFrame` 只跑几帧就停,看着像玩法有 bug。
所以出摊的自动化测试**不靠 rAF**,直接手动调 `_tick(50)` 推进 ——
验的是玩法逻辑,动画归动画。`wa.__svc` 就是给这个留的口子。

**三、一个玩法有开门时间,那「没开门」也是它的一个界面。**
出摊页原来在打烊时直接 `hidden` 掉整个厨房层,结果整页只剩底下两个按钮,
画面和大坝一模一样 —— 玩家点了「出摊」,不知道自己点开了什么。
而我自己的测试全带着 `?hour=15`,正好在窗口里,一次都没撞见。
**给判断加时段的时候,顺手把「不满足时长什么样」也写出来。**

还有一条:时段是会自己到点的。玩家可能就坐在出摊页上跨过 19:00,
这时没有任何一次切页会来通知场景 —— 所以 `syncScene()` 每秒复查一次,
只在真的换了场景时才重绘。

**`hidden` 藏不住东西 —— 这个坑踩过三次。**
浏览器自带的只是 `[hidden] { display: none }`,**一条随便什么
`display: flex/inline-flex/grid` 的类就把它盖掉了**:
抽屉「收起」之后空着不走、飞行里「饿了!快吃」从第一秒就挂在那儿。
前两次都是回来给某个类补一条 `[hidden]`,补一个漏一个。
现在在 `pixel-base.css` 里一次说死:`[hidden] { display: none !important; }`。

**四、「让进度条动起来」不是「重建整个界面」的理由。**
`_tick` 里原来有一条「每 200 毫秒也算局面变了」,为的就是刷火候条 ——
代价是厨房的 innerHTML 一秒重建五次:页面一闪一闪,拖到一半的那张牌
被换成新节点,拖拽当场断掉。**这个玩法的核心动作是拖,而重建正好会杀掉拖。**

现在分成两条线:

    onChange  局面变了才叫一次(来人、走人、上灶、装盘)—— 重建结构
    onFrame   每帧一次 —— 只写 style.width / style.background,不碰结构

实测 300 帧:重建 1 次(原来约 24 次),刷条 300 次。

配套的三件事:

* 拖拽期间**挂起重建**(`ui.dragging`),攒着的那次松手再补 —— 局面变了
  是常有的事(每 7 到 9 秒就有客人来去),不挂起的话总有一次撞在拖的中间。
* **pointer capture 挂在容器上,不挂在那张牌上。** 牌子是会被换掉的,
  换掉的那一刻捕获就丢了;容器一直都在。
* 拖影走 `transform` 不走 `left/top`,高亮只在换了目标时改一次 class ——
  原来每次 pointermove 都把所有厨具清一遍再加回去,手机上一秒几十次重排。

**五、判定框是按场景写的,而三个场景共用一张画布。**
点大坝上的草棚进小屋,判定条件原来写的是「现在不在小屋」。
换成出摊那一场之后,草棚判定框那块地方是案上那摞盘子 ——
**点盘子直接进了小屋**,因为条件只排除了 hut,没排除别的场景。

条件应该是「现在画布上跑的是大坝」(`scene === 'bg'`),不是「现在不在某个页」。
前者是正面条件,以后再加场景不用回来补一条;后者是黑名单,加一个漏一个。

顺带一条美术的:柜台以下占了画面下面 44%,原来是一整块 `#241a13` 纯色,
注释写着「交给 DOM 的厨房盖住」—— 可 DOM 只盖住了厨具那几个盒子,
盒子之间、烤箱右边、操作条周围全露着,一大片死黑,看着像贴图没加载出来。
**「反正会被盖住」这句话得真去看一眼。** 现在那儿按摊子里边画:
板壁、搁板上的坛子、脚下的地面。厨具的空格子同理,原来也是纯黑方块,
现在是深木色凹槽,里面搁一个淡淡的厨具影子。

---

## 六之一·六、飞行:难度得一直涨

原来的难度是「每 5 秒 +0.06,封顶 2.5(三级以上 3.0)」,**一分四十秒就摸到顶**。
之后再飞十分钟也是同一个速度、同一个障碍密度 —— 后半程等于在刷时间,
没有满足感可言。

现在三样跟着**飞行时长**一起涨,而且不封顶:

| | 开局 | 1 分钟 | 3 分钟 | 5 分钟 |
|---|---|---|---|---|
| 速度(px/秒) | 120 | 146 | 199 | 252 |
| 反应时间 | 3.2 秒 | 2.6 | 1.9 | 1.5 |
| 生成间隔 | 860ms | 750 | 530 | 420(底线) |
| 障碍占比 | 22% | 34% | 55%(上限) | 55% |

**一局的终点应该是撞死了,不是不再变难了。** 飞得越久分越高,
但没有「安全地刷」这条路。

### 开局不能是飘着的

第一版的开局速度是 1.25(每 60fps 帧走 1.25 个虚拟像素)—— 一秒 75 像素,
一个东西横穿 440 宽的画面要走**六秒**。玩家第一分钟里几乎不用动手,
而**一个玩法开头的一分钟决定了他要不要玩第二局**。

底盘抬到 2.6,斜率同时从每分钟 +90% 压到 +45%。

但 2.6 又太急了 —— 还没坐稳东西就到脸上。**太闲和太慌是同一个死法。**
最后落在 **2.0**:一秒 120 像素,横穿一次屏 3.7 秒,从露头到眼前有三秒多。

    1.25  横穿六秒,第一分钟不用动手      飘
    2.6   横穿两秒多                      急
    2.0   横穿 3.7 秒,反应 3.2 秒        ✓

等级那一档跟着按比例收(每级 +0.25 → +0.19):底盘降了三成,
每级的加成还按老数走的话,高等级开局会比低等级难出一大截。
**一个跟着底盘走的系数,底盘一动就得重算,不能只改一个数就算完。**

### 从「拖着走」改成两个键

原来是**拖着哇鸥走**:手指在哪它就在哪。够直观,但没有手感可言 ——
位置直接归你,不存在「来不及」,所谓难度就只剩眼力好不好。
而且它和这个玩法要的东西是拧着的:速度、密度一路涨,涨的是「留给你的时间」,
可拖拽根本不消耗时间。

现在中间隔一层重力,三个状态各管一个方向:

    跃起  空格 / 屏幕左半点一下   拍一下翅膀,往上蹿 48px 再落回来
    平飞  方向键右 / 右半按住     按住的时候不掉,横着飘
    掉    什么都不按             加速下坠,终速 6.5px/帧

合起来是完整的上下控制 —— **但每一次改变高度都要提前一点点**,
那一点点就是手感。飞不再是「移到那儿」,是「攒够高度」。

一次跃起升 48 像素,五条道每道 38 像素:**一下略多于一道**。
这个比例是故意的 —— 换一条道靠一下,换两条要连着两下并且提前起手。

### 迟钝不是难度:把弧线按时间压短

第一版的重力是 0.09,弧线又高又慢:拍一下要 **0.54 秒**才到顶,
而且刚拍完想往下走,得先花半秒把那股上冲的劲抵消掉。
于是最常见的死法是「看见了、也知道该往哪躲、就是身子还在往上飘」——
**那不是难,是迟钝**,玩家会觉得是游戏没听他的,不是自己没打过。

改法是**高度不动,把时间压掉一半**:重力 0.09 → 0.30,
初速 −2.9 → −5.4(升的还是 48px),终速 4.5 → 6.5。

| | 改前 | 改后 |
|---|---|---|
| 拍到最高点 | 0.54 秒 | 0.28 秒 |
| 拍完松手,多久开始降 | 0.54 秒 | 0.32 秒 |
| 静止松手 0.3 秒掉多少 | 42px | 51px |
| 从天顶掉到水面 | 1.5 秒 | 0.72 秒 |

**上下都在三分之一秒内起效。** 一秒的预警时间里,躲一条道用掉三分之一,
剩下的是留给「看清楚、想一下」的 —— 这个比例才对。

难度曲线也跟着松:提速斜率 0.45 → 0.32 → **0.22**,
生成间隔底线 300 → 380 → **420ms**。
底线留出来的不是反应时间,是**「拍两下」的时间** —— 换两条道要连着拍两下
并且提前起手,间隔比这个还短,第二下就永远来不及,
那时候躲不躲得掉跟操作已经没关系了。

    反应时间(露头到眼前)  1 级晴天
    0分 3.17s  1分 2.60  3分 1.91  5分 1.51  8分 1.15  12分 0.87

**斜率和底盘是两个旋钮,别混着拧。** 斜率管「多久变难」,底盘管「起点多难」;
开局太慢和后期太急是两个毛病,各修各的。

### 开局速度必须是个常数

底盘调对了之后,玩家还是报「起始速度又莫名变快了」。查下来:
等级和天气是**乘在 `baseSpeed` 上**的,于是「开局多快」跟着它们一起飘 ——

    1 级晴天 120px/s   5 级晴天 166   1 级雨天 156   5 级雨天 215

玩家什么都没干,升了一级、或者今天下雨,开局就快了三成到八成。
而 156 和 166 正好落在之前被判「太急」的那一档上。
**那不是难度设计,那是同一个玩法每次开局都不是同一个游戏** ——
更糟的是他找不到原因,只会觉得手感不稳。

改法和当初的助跑坡一模一样:等级、天气、跑道**一律只加在斜率上**。

    谁飞、什么天、有没有跑道,0 分钟都是 120px/s
    1 级晴 5 分钟 252   5 级晴 305   1 级雨 292   5 级雨 360   1 级晴+坡满 232

> **凡是「一上来就有区别」的系数,都该挪到「变难的速度」上去。**
> 这是第三次撞上同一件事(助跑坡、等级、天气)。玩家能分辨的是
> **变化**,不是绝对值:开局那一下他没有参照物,只有和上一局比。
> 上一局和这一局起点不一样,他一定会觉得是游戏出了问题。

### 开局先数三秒

点完「出发觅食」画面一换,东西就已经在往脸上飞了 —— 玩家还没看清自己在哪儿、
这局是什么天气、两个键长什么样,第一条命就没了。
**一局的第一秒不该用来找自己。**

所以开局先数三秒。这三秒里:

    世界照转    云、浪、翅膀都在动(走 vt,不走 elapsed)
    两个键照用  从第一帧起就是通的 —— 这三秒同时是两个键的练习场
    水面不扣命  倒计时里随便掉
    不计时      距离从 0 开始,难度从 0 分钟开始,不生成任何东西

**它是白送的三秒热身,不是白扣的三秒。**

实现上要分两把尺子:`elapsed` 是「这一局飞了多久」,距离、难度、波数、
饿都从它推,倒计时里必须是 0;`vt` 是画面时间,一直在走,
云和浪和翅膀跟着它 —— 只用 elapsed 的话,倒计时那三秒整个画面是冻住的,
看着像卡了。

数字用 DOM 画,不画在画布上:**画布上没有像素字体,写上去就是一团糊。**

### 窄道:唯一一件非平飞不可的事

两个键里,平飞一直是可有可无的那个。躲障碍靠的是「换到空的那条道」,
而换道用跃起和松手就够了 —— **一个从来不用按的键等于没有**,
两个键的设计等于只落地了一半。

窄道是专门为它造的局面:一整片乌云横过来,只留一条缝,
缝的高度是固定的。你得先飞到那个高度,然后**在里面待住一秒三** ——
松手就掉出去(掉 32 像素只要 0.24 秒),多拍一下就撞上顶。
这一秒三里,平飞是唯一的解。

    每三波来一次(约一分钟),第一次在第 2 波(40 秒)
    提前 1.5 秒预告:一条横穿画面的金色虚线标出缝的高度
    宽度按当时的速度换算成 1.3 秒,不写死像素

**宽度必须换算成时间。** 写死一个像素宽度的话,开局要在里面待三秒(熬人),
后期只要半秒(等于没有)—— 要固定的是「按住多久」,不是「多少像素」。

**不预告的话它就不是考验,是埋伏。** 提前一秒半把缝的位置用一条虚线标出来,
玩家有时间飞过去对准;真正考的是对准之后按不按得稳。

三条配套的规矩:

* **窄道在场上时不再撒障碍。** 云已经把天占满了,再塞东西就是在一条
  唯一的路上设伏。
* **缝里改放吃的。** 按住平飞的那一秒同时也是在进食 —— 这样它不只是
  一道关卡,还接上了第五分钟之后那条肚子条。
* **撞了就把它卷进缝里。** 一片云要走一秒多,不这么做的话挨打的无敌时间
  一过又撞一下,一片墙能吃掉三条命 —— 那是在罚「没躲开」这一个错误三次。

画的时候两条:疙瘩**一律往外让,不往缝里长**(判定的缝是平的,
画面上的缝只会比它更宽 —— 反过来的话玩家会撞上一块看着明明没碰到的云);
两头张成**喇叭口**(云本来就没有刀切的边,而且「对准」和「待住」是两件事,
让人先进得来,再考他按不按得稳)。

### 判定线画在看得见的东西上

水面的判定原来写的是 `HORIZON − 16` —— 算的是「32 高的图,底边正好压在
海平线上」。但**图有 32 高不等于鸟有 32 高**:哇鸥的脚在中心往下约 11 像素,
底下那几行是空的。于是触发的那一刻,它还整只浮在西山那一带的天上,
画面上离水面差着一大截,玩家只看见「无缘无故掉了一条命」。

压到海平线再往下 4 像素(`HORIZON + 4`),触发时哇鸥的身子一半在水里,
像一只落在滇池上的鸥。可飞的高度顺带从 212px 变成 232px。

**判定线该画在玩家看得见的那个东西上,不是画在图片的边界上。**
这一条对所有「贴图比实体大」的东西都成立 —— 影子画接地面、
命中半径按看得见的轮廓,是同一件事的三种说法。

### 界面重排:这本质是个手机游戏

按钮栏原来是**占位置**的:左右各一条竖的、底下一条横的,画面在中间。
桌面上无所谓 —— 但手机横过来也就八百来宽,**左右两条加起来吃掉四成**。

    原来  [栏][ 画面 ][栏]      画面 916px,页面 1096px
          [    栏    ]
    现在  [        画面        ]  画面铺满,四条栏压在它身上

四条栏(状态 / 玩法 / 收集 / 动作)全部**压在画面上**,底色透明。
手指位置一格没变,画面白得了四成宽。

#### 按钮透明,状态不透明

底下三条按钮栏是纯图标 + 字,一点底色都没有,靠给每个字**描一圈深色**读。
但最上面那条状态(钱 / 天气 / 等级 / 经验)**保留边框和底色** ——
试过一版全透明的,字是看得清,但整条看着太素,而且和游戏里别处的 chip
长得不是一回事。

> **按钮只是入口,透明就够;状态是要「读数」的,读数需要一块安静的底。**

描边有个坑:**必须是 1px,不能是 2px**。像素字体在 13px 上笔画本来就只有
一到两个像素宽,外面再糊两圈,字心就被自己的描边填满了 ——
第一版远看整条栏是一串深色小方块,一个字都读不出来。

还有一个更隐蔽的:`text-shadow` 是继承属性,但 `<button>` 在浏览器自带
样式表里被重置了。只在 `.px-rail` 上写的话,栏里的字**一个描边都没有**。

#### 尺寸跟着画面走,不跟着视口走

`.px-screen` 设成 `container-type: inline-size`,栏的粗细和字号全用 `cqi`
(画面自己宽度的百分之一)。**不能按视口算** —— 画面被视口高度夹住的时候
可能只有视口的一半宽,按视口算的话一边一条就又吃掉四成。

画面窄到 620 以下时,竖栏**只留图标不留字**:手机横过来一边一条 44px
已经是手指的下限,字要跟着缩到 9px,而像素字体没有抗锯齿,9px 就是一团。
**认不出的字不如不画。**

#### 整页场景里四条全收起来

小屋和出摊本身就是一整个界面,底下那条场景操作条里有「回大坝」,出得去。
留着反而会和它撞在一起 —— 而且出摊那一场左边正好是烤箱,竖栏会压住它。

#### 底图从 2 倍改到 3 倍

画面能铺到一千二百多宽之后,880 的位图撑到那儿是 1.4 倍**非整数**放大,
像素立刻一大一小。底图跟着上一档(1320×930 = 440×310 的 3 倍),
桌面上就又回到 1:1。

顺带修了一个一直都在的 bug:出摊那四件厨具的判定框写的是 `x * 2` ——
假设画面正好是 880 CSS 像素宽。桌面上碰巧成立,**手机上整块画面是缩过的**,
判定框却还按 CSS 像素摆:看得见的砧板在这儿,收手指的框在那儿。
换成百分比之后,画面缩到多大都对得上。

#### 真机上炸了一次:两个从背后生效的东西

本地按视口截图全对,发到手机上一开是一堆按钮糊在画面正中、抽屉飘在半空。
两个原因,**都不会报错**:

**一、上一版布局的断点还在。** `@media (max-width: 1059px)` 那一整块 ——
那时候三条按钮栏占位置,窄屏塞不下就把左右两条拆成横排摆到画面下面,
抽屉也改成从底下拉上来(`top: 46%`)。按钮栏搬到画面上之后这些全成了反作用,
而 1059 这个断点**把每一台手机和大半笔记本都盖住了**。

> **改布局的时候,旧布局的断点比新布局的 bug 更危险。**
> 新写的哪儿不对一眼看得见,旧的那块是从背后生效的 ——
> 它只是几条 later-wins 的声明,浏览器一个警告都不会给。

**二、`cqi` 和 `dvh` 不是所有浏览器都认。** 容器查询要 Chrome 105+,
`dvh` 要 108+,而国产浏览器的内核经常落后好几档。
坑在于**自定义属性在解析时什么都收** —— `--rail-w: clamp(44px, 5.8cqi, 78px)`
写进去一点问题没有,只有真的代入 `width:` 的那一刻才发现 cqi 不认识,
那时候整条声明作废、回落到 `auto`。四条栏一起塌到左上角,
抽屉的 `top/right/bottom` 全部失效飘到画面正中,**而 CSS 里一个报错都没有**。

所以尺寸写两套:**先一套所有浏览器都认的 px(按视口断点近似),
再在 `@supports (width: 1cqi)` 里整块换成跟着画面走的那一套。**
`dvh` 同理,先 `vh` 再 `@supports (height: 1dvh)`。

验证的办法很土但很有效:把 CSS 里的 `cqi` 全部替换成一个不存在的单位,
`dvh` 也换掉,再截一次图 —— 那就是老浏览器看到的东西。

#### 画面加宽:440×310 → 620×310(2:1)

原来是 1.42:1,而手机横过来是 2.2:1 左右 —— 整个画面塞进去,左右各留一大条,
**画面只占屏幕一半宽,小到没法玩**。

一开始我以为改分辨率等于「每一场构图重来」。**实测下来完全不是。**
直接把 `VW` 改成 620 跑了一遍,结论:

| | |
|---|---|
| **小屋** | **一行没改**,截图直接就是对的(两根柱子本来就一左一右锚着) |
| 大坝 / 出摊的背景层 | 天、湖、长堤、甲板、栏杆、芦苇、雨雾**全部自动铺开** |
| 存档 | 不受影响 —— 存档里没有坐标 |
| 美术素材 | 不受影响 —— 所有精灵都是代码摆位的 |

不是运气,是当初的画法本来就对:背景全写成 `fillRect(0, y, VW, …)` 和
`for (x < VW)`,右边那丛芦苇写的是 `VW - 8`。
**它们从一开始就是「贴着边」,而不是「在第 432 像素处」。**

真正要动的只有**甲板上那十来个写死的内容坐标**:

    摊子 76→96 · 小屋 172→228 · 猫棚 222→300 · 表演位 336→452 · 跑道 396→570
    路灯 3 盏→5 盏 · 缆桩 · 围观右界 374→528
    出摊:四件厨具、排队位、案下两格架子、折耳根

排的顺序有讲究:**摊子和小屋是要点的,放左边(拇指够得着);
表演位在正中偏右(围观的人要往两边站得开);跑道贴着右边缘** ——
它是最后解锁的东西,理应在最远的地方。

飞行那边只改一个数:从露头到哇鸥的距离 380 → 560(+47%),
同样的速度等于白送四成七的反应时间,所以 `SPEED_0` 2.0 → 2.9。
**变的是场地,不是难度;这个数跟着场地走,手感才不变。**

> **这一次的教训是反过来的:** 我先估了一个「等于重构」的成本,
> 差点因此不做。真去试只花了一条 sed 加三张截图 ——
> **能一分钟试出来的事,别花十分钟估。**

顺带:2:1 之后桌面上也不再被视口高度卡住,画面反而更大了。

#### 抽屉改成翻页,不上下滑

滑动的问题不是滑不动,是**看不见头也看不见尾** —— 玩家不知道自己在第几段、
还有多少、刚才那块划过去了没有。手机横过来抽屉正文只剩一百多像素,
这种时候「2 / 3」比一条滚动条有用得多。

分页是**运行时按实际高度装箱**的,不是每一页手写内容:
量一下正文有多高,把顶层的块一块块往里塞,塞不下就翻页。
好处是所有页面(背包、图鉴、摊子、设置……)一次全都有分页,
以后加内容也不用管。

遇到装不下的**网格就地按行切一刀** —— 能塞进这一页的那几行留下,
剩下的自成一块接着排。切的单位是**行**不是**格**:同一行的东西在网格里
是并排的,切到一半会让最后一行忽然变成一个孤零零的格子。

#### 一页 = 一件事:小标题往前看一眼再决定断不断

第一版是纯贪心装箱,结果出现了**一整页只写着「升级」两个字** ——
上一页正好还剩二十像素,标题塞得进去,它底下那四张卡塞不进去。

试过往回捡(一页末尾的矮块推到下一页),没用:下一页要是本来就满的,
推不过去。**问题不在装箱算法,在没定规矩。**

第一版的规矩是**「遇到 `<h3>` 就另起一页」**。治好了落单,却换来另一头的浪费:
「后厨」那一段本来就短(一个标题加一行说明),硬给它一页,底下空掉三分之二。

**两头都堵的写法是往前看一眼**:标题连着它后面那一块,这一页还塞得下就不断页,
塞不下才断。判断的依据是**量出来的高度**,不是「我猜这一段大概多长」。

顺带,「3 / 10」这个数从此对玩家是有意义的:摊子那一页本来就是十件事
(摊位、市场、升级、后厨、餐盘、收集进度、跑道、伙计……),
翻到第几页就是在看第几件,而不是「滚到了大概三成的位置」。

#### 手机上,一段说明的代价是「几分之一页」

摊子那一页顶上原来有一段介绍(摆上去的菜自己会卖、你不在的时候也照卖……)。
桌面上它是一行灰字;**手机上它占掉一页的四分之一,正好把摊位格子挤到下一页去**。
删了 —— 三个 chip 已经把数说清楚,引导里也讲过一遍。

同一轮删掉的还有「后厨」那段说明,和图鉴里每道菜后面那句重复的话。

> **在手机上,说明文字不是用像素计价的,是用「几分之一页」计价的。**

还修了一处一直在的错:那段说明写的是 Markdown 的粗体(两个星号包起来),
而那儿是 `innerHTML` —— **注释里的写法漏到界面上了**,玩家看到的是一串星号。
代码注释和界面文案共用一套强调符号,迟早会串;界面上只用 `<strong>`。

#### 空壳不该和实心的一样高

摊位那三个格子里,锁着的那两个原来和摆了菜的那个一样大 ——
而它里面只有「Lv.3 解锁」五个字。**一行网格的高度是按最高的那个算的**,
两个空壳把整行撑高了一倍。改成一条细的,一行就矮了一半。


#### 够大才谈得上分页

真机上第一版分页出来十一页,因为抽屉当时只是「让开四条栏」——
正文只剩一百二十像素。**那不叫分页,那叫把一页内容剁碎。**

现在矮屏上抽屉贴着机身四边(引导条那一条除外),左右两条竖栏被盖住 ——
反正右上角就有「收起」,出得去。正文从 120 涨到 199。

还有一处反直觉的:试过把格子的 `--min` 从 190 压到 108,
想着「一行多放一个,总行数就少一行」。**结果反了** ——
升级那几张卡里是成句的说明,格子一窄字就换行,卡片不但没变矮反而更高。

> **窄 = 省地方,只对一行能写完的东西成立。**



矮屏上还给抽屉抢了几十像素的高:**上边直接压到画面顶,盖住那条状态**。
抽屉开着的时候鸥币和经验条是看不看都行的,底下那条按钮栏才是必须留的
(不然换不了页)。状态条 z-index 比抽屉高,所以还得在 JS 里把它收起来。

#### 新手引导从抽屉里搬出来

它原来是 `#panel` 的第一个孩子 —— 一条四行的说明就吃掉抽屉一半的高度,
而抽屉本来是用来看背包和摊子的。**「引导告诉你去点什么」和「你要点的
那个东西」在抢同一块地方。**

现在压成画面底下的一行便签:序号 + 标题 + 一句话,读不完的截断。
它指的东西就在下面一行,而它自己只占一行。

让开的高度**是量出来的,不是写死的**(`--coach-h`)—— 引导条的高随字号和
断点变,写死一个数迟早差几像素,而差几像素的表现就是「翻页按钮点不着」。

#### 一条 `#panel { min-height: 320px }` 藏了两轮

上一版布局留的:那时候抽屉是往下接的一长条,给个下限免得它空着太薄。
按钮栏搬进画面之后,它成了**「内容漏出抽屉」的真正原因** ——
`pixel-ui.css` 里给 `.px-drawer__body` 写的 `min-height: 0` 是类选择器,
被这条 id 选择器压得死死的,于是 `overflow: auto` 永远不生效。

> **两边都写着 `min-height`,看着像是已经处理过了。**
> 这类 bug 最难查的地方在于:出问题的那条和修它的那条长得一模一样,
> 眼睛会自动认为它们是同一件事。查的时候要看的是 **computed style**,
> 不是源码里写了什么。

#### 顺手减掉的重复文字

图鉴里每道菜后面都跟着一句「摆到摊上自己卖」—— 九道菜九遍,而且它对每道菜
都成立,等于什么都没说。换成一个「能亲手做」的小标签(只有能出摊做的才有),
剩下的说明砍掉。摊子、设置、背包那几段介绍也各砍了一半。

**说明文字的判断标准:去掉它之后,玩家会不会做错事?** 不会的就是装饰,
而在手机上,装饰是要用画面换的。

#### 地址栏:manifest + 一次全屏请求

手机浏览器那条地址栏在横屏下要吃掉三分之一的高度,而**高度每省 1px,
画面就能宽 2px**(2:1 的反解)。所以两条路一起走:

* **`site.webmanifest`**,`display: fullscreen` + `orientation: landscape`。
  「添加到主屏幕」之后从桌面图标进来就是一个全屏横屏的 app,地址栏整个没有,
  连竖屏提示都用不上了。
* **点「开始游戏」那一下顺手 `requestFullscreen()`。** 全屏只能在用户手势里
  请求,而这一下正是唯一一个保证有的手势。设置里另留一个开关,
  免得玩家退出全屏之后找不回来。

**全都得容错**:`requestFullscreen` 在 iOS Safari 上对非 `<video>` 直接没有,
`orientation.lock` 只在全屏里有效而且好几个浏览器没实现。
一处失败不该把「开始游戏」这一下也带崩 —— 拿不到就当没这回事,
玩家最多是多看一条地址栏。

#### 两个只在矮屏上现形的老 bug

**一、`.px-drawer__body` 少了 `min-height: 0`。**
flex 子项的 `min-height` 默认是 `auto`,也就是「不小于内容高度」——
于是 `overflow-y: auto` 根本不生效:它不缩,直接把内容顶出抽屉外面。
桌面上抽屉够高、内容大多塞得下,所以一直没露馅;手机横过来抽屉只剩两百来高,
背包里那几行就漏到底下那条按钮栏上。
**这是 flex 里最经典的一个坑,而且它不报错,只是滚不动。**

**二、飞行画布的尺寸没跟着 `VW` 走。**
画面从 440 加宽到 620 那次漏了 `flight.js` 的 `W/H` —— 画布还是 1320 宽,
而 `PixelScreen` 按 620 算出的整数倍是 2,于是 620×310 的图以 2 倍贴成
1240×620,右边 80、底下 310 全是**透明的**,大坝那张背景画布从底下透出来:
飞行画面下面三分之一是甲板和猫。真机上看着像「飞着飞着掉进大坝里」,
而代码一个错都没报。

> **一份分辨率,三处要跟着改**(`scene.js` 的 VW/VH、三个 canvas 的
> width/height、`flight.js` 的 W/H)。它们之间没有任何强制关系,
> 全靠记得 —— 这是这次唯一一个我认为迟早还会再犯的地方。

#### 抽屉在矮屏上撑满两条竖栏之间

竖着只剩两百来像素,再留 58% 的宽,内容就得挤成一列细条,一屏放不下三行字。
**横向本来就是这个姿势里富余的那一维 —— 该省的是高度,不是宽度。**
左边那条竖栏照样露着,换页不受影响。

#### 竖屏:请把手机横过来

画面 1.42:1,大坝本来就是长的。竖屏硬塞的话画面只能缩到手指盖大小,
不如直接请玩家转一下。`screen.orientation.lock()` 只在全屏里有效、
而且 iOS 不支持,所以**不锁,只提示** —— 提示是所有浏览器都吃的那条路。
纯 CSS 判定,转回横屏它自己就没了。

#### 顺手搬了三样东西

| 原来在哪 | 搬到哪 | 为什么 |
|---|---|---|
| 瓶盖(顶栏) | 背包 | 它是一种资源,而且只有一个去处(装扮),不用一直盯着 |
| 觅食次数(顶栏) | — | 底下「出发觅食 5/5」那个按钮上本来就写着,顶栏那份是重复的 |
| 音效开关(顶栏) | 设置 | 每局点一次的东西,不该占一屏里最贵的位置 |
| 图鉴/明信片/成就进度(右栏) | 摊子 | 「隔几天看一次」的东西。栏透明之后,多三行字就多糊住三块画 |

顶栏最后只剩三样:**钱、天、等级+经验**。判断标准是「要不要一直盯着」。

新开一页**设置**(齿轮图标),放音效开关和音量。音量原来根本没得调,
只有代码里写死的一个数,而「声音太大」是手机上最常见的一条抱怨 ——
而且玩家的第一反应就是找齿轮。

音量滑块有个细节:**拖的过程中不能整页重绘**。重绘会把 `<input>` 重建一遍,
手指按着的那个滑块当场就没了,拖到一半永远断在那儿。所以只改那个百分比标签。

### 屏幕上那两个键:一边一个,圆的,而且要大

第一版把两个键并排放在左下角。两个毛病:

* **手机上根本点不到。** 用的是 `px-btn--sm`,真机上只有指甲盖那么大,
  而平飞是要**一直按住**的 —— 按不准就等于这个键不存在。
* **挤在同一边,一只手要横着够两个。** 键盘上空格在左、方向键在右,
  屏幕上就该是同一套位置关系:左下角跃起,右下角平飞,一个手一个。

现在是两个直径 96px 的圆(窄屏 80px),`touch-action: manipulation`
挡掉连点触发的双击缩放,`user-select: none` 挡掉连点选中文字。
暂停/收工那两个小按钮挪到右上角,状态条让开它们并且允许换行 ——
窄屏上状态条还得收一档,五个 chip 按原尺寸要占三行,
而**状态条吃掉的是能飞的地方**。

#### 像素画里没有真圆

`border-radius: 50%` 会给边缘抹一圈抗锯齿的灰 —— 整张画面里就这两个地方是糊的。
所以走 `clip-path`,轮廓是 16×16 像素圆那条阶梯:边缘全是直角,放大多少倍都不糊。
(和画布上 `plate()` 拿切角方块当「圆」是同一个道理。)

三个都是踩出来的坑:

* **直径必须是 16 的整数倍。** 96/16 = 6,每一级正好 6 个 CSS 像素;
  88 的话每级 5.5px,浏览器会给每一级抹一道灰。里圈同理:缩 8px 之后是 80,
  80/16 = 5,还是整数。
* **厚底不能用 box-shadow。** `clip-path` 会把它一起剪掉。
  `drop-shadow` 是跟着剪完的形状走的,所以圆边下面才有那一道厚。
* **明暗不能用 inset box-shadow。** inset 阴影贴着方盒子画,剪成圆之后成了
  顶上一道平盖、底下一道平托,像给硬币扣了个盒盖。改成 `linear-gradient`:
  先铺满再一起剪,亮暗跟着圆边走,才像个球。

### 吃的判松,撞的判紧

食材和障碍原来共用一个 17 的命中半径。后期一个东西一帧要走十几像素,
从露头到擦身而过只剩两三帧 —— 同一个数字下,「差一点没吃到」和
「差一点没撞上」出现得一样多,但**玩家只会记住前者**:
没吃到是我的损失,没撞上是理所当然。

所以拆开:食材 22(比画面上那颗看着还大一圈,伸头就够得着),
障碍 15(擦着边过去算你过去了)。道具跟食材走。

### 只给危险的那一类做记号

后期一秒钟从眼前过四五个东西。原来三类全是光秃秃一张 16×16 贴上去,
要判断能不能碰,得先**认出这是饵块还是风筝** —— 谁也来不及一个个认。
飞一会儿眼睛就花了。**该让人认的是「这一类」,不是「这一个」。**

第一版的答案是三档各给一个徽记:食材奶白细边、道具白盘金边、障碍深底红边。
辨识度是有了 —— **代价是满屏白块红块,天空全被糊住,画面比玩法还吵。**

第二版把它砍掉一半:

    食材  光板一张,什么都不加      能吃
    障碍  四个红直角框住            碰不得
    道具  白盘 + 金边(本来就有)   能吃,而且是好东西

**只有一类需要记号。**「有红角的不能碰,没有的能吃」是一条完整的规则,
而它只花掉四个直角的墨。加装饰解决辨识度是对的,但**两边都加就等于都没加**,
还白搭进去一整片天。

红角落在**判定框的四角**上(半径就是当前的判定半径)—— 顺带把
「看着碰上了」和「算作碰上了」对齐了。每个角先用深褐描一遍再压红:
红在白天的浅蓝天上够跳,在黄昏那片橙红里会糊,垫一道深褐就哪种天都站得住。
整张画面里只有这一处是纯红,它专管一件事:别碰。

臂长 4,**不到半边**。第一版画到 6,四个角快连成一圈,天上又是一片红方块 ——
比不做记号还吵。4 就够了:**眼睛会自己把四个直角补成一个框**,
这是白送的,画满一圈只是把天空还回去。

### 安家费 + 工钱:伙计要养,不是买断

原来招人只有一笔钱,六只全招齐一万零四百 —— 数值模拟里休闲玩法**第三天就够了**。
于是伙计不是一个「要不要养」的决定,是一个「早晚都会点」的按钮:
**一次性的支出,再贵也只贵那一天。**

现在拆成两笔:

    安家费   招的时候一次性 —— 让它从鸥群里专程飞过来落脚
    工钱     从此每天一份,跨天自动扣

    灰灰 2000/80   阿胖 3500/140   小白 6000/240
    老翘 10000/400  墩墩 15000/600  丫丫 22000/880
    六只全在:安家费 58500,每天 2340

安家费大约是工钱的 25 倍 —— 招进来那一下相当于先垫了一个月,
之后这只伙计就一直挂在你的日常开销上,和摊子一样要养。

**季节只加安家费,不加工钱。** 贵的那部分是「捎信让它专程飞一趟」的路费,
工钱是干活的钱,和它当初怎么来的没关系。

#### 发不出工钱怎么办:请假,不是辞职

三种做法:辞退(狠,而且会雪崩 —— 你正是靠他们赚钱的)、
记成欠债(玩家看不见,还完之前一直闷着)、**请假**。

选请假:没发出工钱的那几只今天不来上工,所有加成都不算,
**坝上也看不见它们** —— 加成关了人还站在那儿的话,玩家会以为是 bug。
第二天照常回来,一个都不会丢。

三条配套:

* **按招进来的先后发,先来的先拿到。** 钱不够时得有个说法,
  而「先来先得」是最好解释的一种;换成「贵的先发」或者随机,
  玩家都会觉得被算计了。顺带它也让人数最大化 —— 先发的都是便宜的。
* **有个「补发」按钮。**「今天没钱」不该等于「今天白过」:
  出一趟摊挣到钱,马上就能把人叫回来,不用干等到明天。
  **惩罚要有出口。**
* **扣了钱必须说一声。** 一觉醒来少了两千鸥币而屏幕上什么都没说,
  那和 bug 没区别。跨天那一下和离线收益一起排队弹。

发工钱写在 `refreshDaily()` 里面,不拆出去让调用方自己叫 ——
现在有三处会跨天(主流程、数值模拟、`wa.nextDay`),
拆出去迟早漏一处,而漏了的表现是「有的时候不用发工资」。

#### 还差什么

后期这笔钱是噪声:休闲玩法第 30 天有两百多万鸥币,一天 2340 等于不存在。
但那是整条后期曲线的问题(什么开销到那儿都是噪声),
这一版先把**前中期**的决定立住 —— 数值模拟里招齐六只从第 4 天推到第 11 天。

### 老翘和丫丫:把伙计接到觅食上

六只伙计原来全在管摊子(出餐快、卖得贵、离线少亏……),
于是**觅食那边招不招人一个样** —— 而觅食恰恰是每天都要点开的那个玩法。
这两条把它接上,而且各接各的性子:

    老翘  飞了十二年,一眼看出哪片云能穿
          障碍判定 15 → 12,红角臂长 4 → 7 并压一道亮线
    丫丫  歪着头四处嗅,总是先一步撞上东西
          每 30 秒替你挡一次;挡下来的时候她自己从前上方探出来

都是**加在原来那条加成上的,不是换掉** —— 老翘照旧每次多带 1 个,
丫丫照旧稀有多出 50%。

三条设计上的讲究:

* **判定缩小和标记加长必须一起做。** 只缩判定,玩家根本感觉不到
  (他看见的还是那么大一块);只标清楚,又只是好看。
  框有多大判定就有多大,所以招了老翘框会跟着缩 —— 他帮的忙是**看得见**的。
* **先花会回来的那个。** 撞上时先用丫丫再用护盾。反过来的话,
  玩家会在丫丫满着的时候白扔一个护盾,而那个护盾是他花钱买的。
* **掉进湖里丫丫不管。** 她挡的是迎面来的东西,沉下去是自己没拍翅膀 ——
  一个连自己失误都替你兜的伙计,玩家很快就不看屏幕了。

丫丫那条还得**在 HUD 上报**(现在能挡 / 还有几秒):
看不见的保命等于没有,玩家要么白白紧张,要么以为还有、结果没有。
而挡下来那一下画的是**她本人**,不是一圈光效 ——
多花的三千八百鸥币要看得见回来,一个只在数字上生效的伙计,和没招是一样的。

(食材反过来:判定 44 宽而画面上什么都没有 —— **白占便宜不用告诉玩家,
吃亏才要。**)

### 两个东西不能出在同一个 x 上

雨天多撒一簇障碍、雾天多撒一份食材,这两处「加塞」原来和主生成用的是
同一个 `x = VW + 16`。于是雨天雾天常常出现一个障碍和一份食材叠在一起 ——
两个圈重在一处,玩家看见的是一个障碍,伸头去撞的是一份饵块。

而且生成的疏密本来就是靠 x 上的间距做的,**加塞的那个不错开,
等于在破坏节奏本身**。现在加塞的一律往后半屏,食材落点再兜一道:
挑高度时试六次,躲开还挤在右边缘的东西。
三种天各 3000 次生成,压在一起 0 对。

### 这一场里的装饰云得退到背景去

障碍之一就是乌云,而天上本来就飘着三层装饰云。入夜之后 `shadedSprite`
把装饰云也压成灰的 —— **两者在天上一模一样**,玩家分不清哪朵能穿、
哪朵不能。

所以飞行这一场单独把装饰云的透明度压下来(夜 0.42 / 黄昏 0.6 / 白天 0.8)。
大坝那一场不动:那儿云只是云,没有歧义。
**同一个素材在不同场里的地位不一样,画法就该不一样。**

### 食材出在够得着的地方

原来食材在整片天里均匀抽。后期一个东西从露头到擦身而过只有一秒出头,
**抽在另一头的那些从出生起就吃不到** —— 玩家看着它飘过去,
学不到任何东西,只觉得这游戏在馋他。而第五分钟起还有一条要靠吃东西
才撑得住的肚子条,这就不只是可惜,是直接判死。

现在以哇鸥当下的高度为中心、上下各 72 像素(约两下翅膀)抽:
够得着,但得动。而它飞过来的那一秒里哇鸥自己也在动,所以并不会变成
「张嘴等着」。**障碍不跟着走** —— 跟着走就成了追着人扔石头,
它靠的是「一簇里必留一条空道」那套规矩。

### 平飞为什么可以是免费的

第一反应是给平飞加个体力条,不然按住不放不就无敌了。**但它不是无敌**:
障碍是成簇来的,每簇随机留一条空道,你必须换道。
平飞只解决「停在原地」,而原地恰恰是最经常会死的地方。
真正稀缺的从来不是停,是**在正确的时刻已经在正确的高度上**。

加一条体力槽只会多一个要盯的东西,并不改变这件事。

### 水面:扣一条命,自己蹬水起飞

有了重力就有了下边界。做成「碰到就结束」的话,配上重力,
玩家会不敢往下飞 —— 而下半屏的食材是要人去捡的。

所以掉进滇池扣一条命,然后自己蹬水起来,弹得比空中拍一下有力得多
(升 120px 而不是 45px)。**一次失误只会是一条命**:弹得矮的话,
还没回过神就又拍下去了,一个走神扣两三条,那是在罚玩家没盯着屏幕,
不是在考他会不会飞。

设定上也对得上 —— 落在滇池上再飞起来,本来就是海鸥每天在干的事。

### 屏幕上那两个按钮就是说明书

画布左右半边都认(左半点一下=跃起,右半按住=平飞),
手机上满屏可点比去够一个小按钮靠谱得多。但按钮还是画出来了,
因为它同时是**唯一一处告诉玩家有这两个键的地方** ——
上面写着「空格 跃起」「→ 平飞」,键盘玩家看一眼就知道按哪儿。

两个坑:

* 屏幕按钮必须听 `pointerdown`,不能听 `click` —— 平飞要的是「按住多久」,
  而 click 到松手才发,那就永远只按得住 0 毫秒。
* 按下时要 `preventDefault()`,否则按钮抢走焦点,空格就落到按钮上
  变成「再点一次这个按钮」,键盘和屏幕两套操作会打架。

还有两处是**没有事件可听**的:系统的键盘自动重复(压着空格不放会一秒发
几十个 keydown,每个都拍一下就直接飞上天,所以 `e.repeat` 一律丢掉),
以及切走窗口时浏览器不补 keyup(不听 `blur` 的话,回来会发现平飞一直按着,
而玩家手上什么都没按)。

### 难和不讲理只隔一层

密度是后半程难度的主要来源,但满屏障碍没有缝的话,玩家学不到任何东西,
只会觉得游戏在耍他。所以可飞的高度切成**五条道,一簇障碍必留一条空的**,
留哪条随机 —— 还是得看、得躲,只是保证躲得掉。

一簇里放几个跟着波数走(`1 + wave/4`),最多放四个,那条空道永远在。

### 升档要看得见

每 20 秒一波,到点报一次:响一声,画面上横过一道金光,金光上面用短竖条
数出这是第几波,HUD 上也有个「第 N 波」。

**悄悄变难只会让人觉得「我怎么突然打不过了」,而不是「又上了一档」。**
这两种感受差的就是满足感本身。

### 第五分钟起:饿

光是「越来越快越来越密」还有个漏洞:最省事的活法是**贴着一条空道一直飘,
一口都不吃**。越往后越难的曲线,养出来的反而是最无聊的打法。

所以第五分钟起加一条一直在掉的肚子条:

    空条撑 13 秒        什么都不吃的话
    吃到一样补 0.17     大约每两秒得吃到一个
    掉光 = 飞饿了回巢

**从这一刻起,躲和吃得同时办** —— 而这两件事是打架的,食材和障碍在同一片天上。
到第五分钟障碍已经占了 55%,想吃就得往人堆里钻。

饿回去**不算摔了**(`survived: true`),收获照拿:它是自己决定回巢的,
不是掉湖里了。这一条很重要 —— 罚一个撑了五分钟的玩家,等于告诉他别撑那么久。

肚子条画在画面正上方横贯一条,快见底时整条闪。这时候玩家的眼睛全在障碍上,
不闪根本看不见。

### 距离就是时间换算的 —— 而且得让人看见它在涨

**改了算法不等于改了玩家看到的东西。** 距离改成按时间算之后,屏幕上那个
米数还是一跳一跳的:碰到道具才蹦一下,平时纹丝不动。

原因是 HUD 只在 `_emit()` 被调用的时候刷,而 `_emit()` 只在
「捡到东西 / 挨了一下 / 上一档」这几处叫 —— 全是事件。一个按时间连续
增长的量,报给界面的时机却是离散事件,玩家一眼就看得出来那不是在飞,
是在数捡了几个。

现在 `_emit()` 挪到 `_update()` 末尾,每帧报一次,事件里那六处全删掉。
一帧只是改几个 `textContent`,不重建任何结构,这个代价可以忽略
(和出摊那边「结构走 onChange、数值走 onFrame」是同一条道理)。



**不累加实际位移。** 原来是每帧把移动量加起来 —— 而后期速度是开局的
三四倍,同样撑一秒记的距离就差三四倍,「飞了多远」变成了「后期占多大便宜」。
这个玩法真正考的是撑了多久,那就直接按时间算:一秒 40 米。
数字匀速往上走,读起来也踏实,成就的门槛还能直接换算成时间
(2000 米约 50 秒,8000 米约 3 分半)。

### 记录和成就

存档里记 `stats.bestDist` / `stats.bestWave` —— **只记最好的一次**。
「最远飞过多少」和「一共飞了多少」是两件事,后者是 `totalScore`,
成就要看的是前者。破了纪录才弹一条,每局都报「本次 300 米」是噪音。

配套四条成就:2000 米、8000 米、撑到第 10 波、撑过第 5 分钟(第 16 波)。

### 要调的话

`js/game/flight.js` 的 `_difficulty()`,三个系数:

    f.speed         = f.baseSpeed * (1 + mins * 0.9)    // 提速有多猛
    f.spawnInterval = max(260, f.baseInterval - mins * 280)
    f.hazard        = min(0.55, 0.22 + mins * 0.12)     // 障碍占比

**这三个数是我拍的,没有实测。** 和火候那两个数一样,得你飞几局才知道。

---

## 六之一·七、瓶盖:钱买不到的那一段

瓶盖原来只有装扮一个去处 —— 一样只能换帽子的货币,拿到手也就那样,
成就那一整页跟着一起变成了「顺手拿的」。

现在它管的是**四条摊位升级线的最后三级**:前面九级鸥币能堆出来,
最后三级堆不出来,得拿成就换(2 / 4 / 6 个)。

「钱买不到的那一段」比「又一个数字」有意思:

* 成就从一张清单变成了通往满级的必经之路
* **不用往数值曲线上再加一个无底洞** —— 升级线还是那么长,只是最后一截
  换了一种钥匙。加档位是最省事也最难验的做法,这个不是。

### 这笔账要对得上

| | 个数 |
|---|---|
| 成就总共给 | 135 |
| 装扮花掉 | 68 |
| 四条线的最后三级 | 48 |
| 富余 | 19 |

外加「换羽」那个大坝事件偶尔掉一个,所以不会硬锁死。
**改 `upgradeCaps()` 或者动成就档次之前,先把这笔账重算一遍** ——
供给是固定的,多开一个口子就有人买不齐。

---

## 六之一·八、天气得改变画面里的人在干什么

原来下暴雨打雷,坝上的人照样溜达、猫照样直挺挺摊在露天地上,
半夜三点和早上八点一样热闹。**天气和时段只改了调色和一层雨丝,
底下「大家在干嘛」一动不动** —— 越看越假,比不加天气还怪。

现在三件事跟着走:

### 人数跟着钟点

| 时段 | 晴 | 雨 |
|---|---|---|
| 07–09 早上遛弯 | 3 | 2 |
| 10–11 | 2 | 1 |
| 12–15 正午晒 | 1 | 1 |
| 16–19 傍晚 | 3 | 2 |
| 20–22 | 2 | 1 |
| 23–06 | 0 | 0 |

下雨少一个,但**白天至少留一个**:一个人都没有的大坝看着不是「下雨了」,
是「这游戏没做完」。深夜本来就该空着,那时候是 0。

### 下雨天各撑一把伞

路人、围观的、哇鸥都撑。伞返工过一次:第一版只画伞面、直接扣在脑袋上,
17 格宽比人只宽四格 —— **读出来是一顶彩色的帽子**。
现在 21 宽、伞面抬到头顶上方几格,中间一截柄把两头连起来。
「伞悬在头上方」这层关系画出来了才是伞。

哇鸥的伞画在装扮之后 —— 它得盖在斗笠上头,不然帽子会从伞面里穿出来。

### 折耳根有个窝了

摊子旁边加了一间敞口的小木棚(`tools/shed.py`)。晴天她摊在棚门口,
下雨躲进去。**棚子正面必须是敞开的** —— 封起来就看不见里头的猫,
而「她躲雨去了」这件事全靠看得见才成立。

### 天气得下到每一场

大坝那一场一直画雨和雾,**出摊那一场从来没画过** —— 于是雨天的出摊界面
是一整片没有雨的灰,看着就像贴图坏了。同一套 `drawRain` / `drawFog`,
补一行就行,只是一直没人想起来还有第二个场景。

雨只下到栏杆那一线为止:柜台以里是棚子底下,淋不着。

### 板缝的方向决定这个面是躺着还是立着

甲板原来画的是**竖**缝。竖缝铺在一片平贴的木色上,人眼直接把它读成
一堵板壁 —— 于是站在上面的客人像贴在墙上,摆在上面的小木棚像漂着。
「透视很怪」「像漂在码头上」说的都是这一件事。

判断一个面是躺着还是立着,人眼靠的就是缝的方向。改成**横**缝、
缝距往前逐渐加大(最省事的近大远小),这个面立刻躺下去了。

配套还要两样:

* **前后明暗**:贴着栏杆那头在阴影里,靠近前沿那头受光
* **接地阴影**:出摊那一场的客人一直没有影子(大坝那边的人都有)。
  没影子的东西不管画得多好,都是**贴**在背景上的。
  46 格宽的小木棚没有影子,比一个人没有还明显。

## 六之一·九、跑道:给觅食开一条投入线

觅食(飞行)是每天都要点的那个玩法,但它一直**没有任何可投入的东西** ——
鸥币全花在摊子和厨房上,飞得好不好只看手。而哇鸥每天就地一蹬就走了,
起飞的地方什么都没有。

跑道把这两件事一起解决:一座小高台,建起来要 3000,建好之后三条线各自升级,
每条对着飞行里一个具体的参数。

    助跑坡  提速的斜率 −3%/级   变快来得晚一点(满级 −15%)
    风向旗  障碍占比 −2 个点/级
    食槽    肚子条 +15%/级      管的是第五分钟之后那一段

满级和没建的差距:五分钟处 507 → 454 px/秒,开局障碍 22% → 12%,
肚子 13 秒 → 22.75 秒。

助跑坡满级封在 −15%:再多就等于把整条难度曲线往后推一大截,
而**一条能把玩法难度买掉的升级线,本质上是在卖「不用玩」**。
15% 是「多撑一程」,不是「换一个游戏」。

**助跑坡减的是斜率,不是速度本身。** 第一版按比例砍开局速度,
结果是全程一个固定折扣:开局更慢(而开局本来已经太慢了),
从头到尾一个手感,玩家买完只觉得「更飘了」,**根本看不出这钱花在哪** ——
一个从不变化的加成,和没有加成是同一种体感。
改成减斜率之后,同样的开局、同样的手感,但你能多撑一程,
而那一程正好记在**最远距离**上,看得见。

**没建的时候画面上也有东西**:地基和两根空杆(`runway_off`)。
一条玩家看不见的升级线等于不存在 —— 先让他看见这儿会有个东西、要多少钱。

### 跑道从「看台」改成「杆子」

第一版画的是高台 + 坡道 + 两面旗,越改越不对:台子加高就挡住后面的湖,
旗子做大就和站在台上的鸟互相遮,而且那一整块**横着**占了坝子右边一大片 ——
本来就挤的甲板更没地方了。旗子朝里朝外、台面加宽、鸟往哪挪,
全是在同一个死结上打转。

换个思路就散了:**它是个高空起跑点,不是一座看台。**
一根杆子往上戳,顶上一间小屋(放大的鸟窝箱),屋顶一支风向标。

    竖着占地方  杆子只有 4 格宽,甲板上几乎不占位置
    轮廓独一份  坝上别的东西全是横的,只有它是竖的,一眼就找得到
    说得通      起飞点当然要高;风向标也正好对上「风向旗」那条升级

屋子在杆顶那一带是水面,和甲板上的东西谁也不碍着谁 —— 前面那些位置冲突
一次全没了。**改不动的时候,先问是不是形状选错了,而不是继续挪。**

栖木要**往左伸出去一大截**:一只 16 格宽的鸟挨着 18 格宽的屋子,
不伸出来的话它半个身子压在门洞上。

### 客人只点你今天做得出来的

原来客人是从「解锁了的菜」里随机抽 —— 于是常常有人点一道你根本缺料的菜。
那一单白占一个位子四十多秒,而玩家除了干等什么也做不了。

**这不是难度,是运气惩罚。** 出摊的时段本来就短,一个死单能吃掉十几分之一的
营业时间,而它和玩家的任何操作都无关。

改成**七成点得出来的、三成随便点**(`READY_BIAS = 0.7`)。
现实里的摊子也是这样:今天没有的牌子上大半不写,但也总有人进来问一句。

为什么不是全给做得出来的 —— 那样太顺,出摊就成了照着单子做,
没有「哎呀这个没料了」那一下。**留三成是留压力,但它现在是可以准备的压力**:
玩家知道备料越全、被问倒的次数越少,这就和纯运气不一样了。

实现上有个顺序要注意:**先按比例挑池子,再看池子空不空**。
反过来写的话,「没有做不出来的菜」那种情况会白白吃掉三成的抽签。
实测:只做得出一种时 69.4%、料比较全时 70.2%、料全满时 100%。

### 架子上的东西:少画几件,而且要站在板上

案子底下那两格返工过一轮,两个毛病是连着的:

**一、东西太多太小。** 原来塞了十五件(三摞碗、三个米袋、三个竹筐、三层蒸笼、
煤气罐),每件只有二十来格,挤在一片暗色里全成了「带条纹的方块」。
**少画几件、每件画大一点,反而看得清是什么。**

**二、透视对不上。** 上面的案面是往前递进的横板,这儿却是完全正面的平面 ——
一个说「稍微俯视」,一个说「正对着看」。修法是让每层隔板**露出顶面**:

    先画板的顶面 → 东西摆在顶面上 → 最后把板的前沿压在东西的脚上

最后那一压就是「它站在板子上」那句话。少了它,东西是贴在背板上的贴纸。

### 影子的宽度跟着「落地的那块」走

跑道的影子原来写的是 40 格宽 —— 那是屋子的宽度。可屋子在杆顶,
真正落在甲板上的只有 12 格宽的底座。于是一根 4 格宽的杆子底下摊着
一片比它宽十倍的影子,一眼就不对。

**影子画的是接地面,不是这张图有多大。** 同理,底座要有厚度:
一块平铺的板子读不出「立在地上」,得能看见顶面和侧面两层。

还有一条:丫丫原来站在杆子**正前面**,把底座整个挡住了 ——
「杆子立在甲板上」这句话全靠那一小块底座说,挡住就没了。挪到旁边。

### 天黑了大家都回去

折耳根钻木棚、哇鸥回草棚,而伙计原来整夜站在坝上颠锅收钱 —— 说不通。
**一个会睡觉的世界里,不能只有主角睡。**
伙计不住坝上,所以是直接不画(`phase === 'night'` 就返回),
不像猫那样换个地方待着。

### 一块甲板放不下所有东西

大坝那 440 格宽已经排满了:摊子 34–118、草棚 159–184、木棚 199–245、
跑道 342–398、三根路灯、两丛芦苇,再加哇鸥表演那一圈人(218–374)。
这一版挪位置踩到的全是这个:

* 小白翻跟头要带俩围观,**左边那段塞不下** —— 原来放 152,
  右边那个观众正好站在草棚门口把小屋挡了。整组挪到右半边空地(300)。
* 跑道搬过来之后,右边那丛芦苇(8 根,一直盖到 386)把台上那面旗埋进草里。
  **前景要有,但不能把新盖的东西吃掉** —— 芦苇收到 5 根。
* 跑道两面旗原来都朝里飘,正好盖住台面中间 —— 而那是老翘要站的地方,
  于是「鸟把旗挡了」和「旗把鸟盖了」轮流发生。**改成各自朝外**,中间就空出来了。

### 灯光要正好盖住窗芯

草棚夜里透出来的光原来写的是 `(cx-5, deckY-25, 10, 6)`,而窗芯在精灵图里是
局部 x 5–14、y 9–13,换算成画面坐标是 `cx-8` 起、10 宽、5 高。
差的那三格全糊在右边的窗框上 —— 看着就是「灯光边缘和窗框重叠了」。

**贴在精灵图上的光斑,坐标要从图里数出来,不能凭感觉写。**

### 伙计各干各的,不排排站

六只招进来之后原来在坝上一字排开、一起晃 —— 像罚站。
现在各站各的地方、各干各的事,而且干的正好是卡片上写的那件:

    灰灰  举着锅上下颠           「我翅膀有力,颠锅归我」
    阿胖  身前一枚鸥币转来转去   「我认得出谁兜里有钱」
    小白  原地翻跟头,自带俩围观 「我会翻跟头。真的」
    墩墩  站在**摊子顶上**,纹丝不动 —— 只有它一动不动,这就是它的动作
    老翘  站跑道台面上望着湖面
    丫丫  低头嗅一阵,抬头看一下

墩墩站的位置要**现算**:摊子的高度跟着升级变(四个阶段 28~76 高),
写死一个偏移量的话,升一级它就悬在半空或者陷进屋顶里。

### 招了人得看得见

六只伙计鸥原来只是六条被动加成 —— 花几千鸥币招一只,画面上一点变化都没有,
那笔钱花得没有着落。**放置类游戏里,花钱最该买到的东西之一就是「看得见的变化」**,
摊子随升级变样是这个道理,招人也是。

现在招了谁,谁就站到坝上自己那个位置(`CREW_SPOT`),按干的活儿分:
颠锅的挨着摊子,看摊子的守另一头,老鸟靠栏杆看水。
都站**最后那条道**(和木棚同一线,贴着栏杆)—— 那一带本来就空着,
而且客人和路人都在它们前面,谁也挡不住谁。

用的就是卡片上那六个 16×16 图标:哇鸥在坝上也是 16×16 的那张,尺度天然对得上。

### 招人不再是「冬天才行」,是「别的季节贵」

原来 `canHire` 只有冬天为真。设定上没错 —— 鸥群冬天才来昆明 ——
但代价是**一年里七个月这套系统整个碰不到**:六只伙计、六个图标、六条被动,
夏天打开游戏的人一样都看不着。对一个别人可能只玩一次的游戏来说,这个代价太大。

改成按季节加价(`SEASONS.hireMul`):冬天原价,秋天 1.5 倍,春天 2 倍,
夏天 2.5 倍。说法是「托人捎信,让它专程飞一趟」。门不关死,
但冬天依然是划算的时候 —— 界面上直接写「冬天只要 400」,把等待变成一个选择。

### 前景是有代价的:芦苇会把人吃掉

大坝那一场的芦苇画在**所有人之上** —— 被前景挡住一点才有纵深,这条没错。
但右下角那丛占着 x≥386,而围观的人是以哇鸥(x=336)为中心往两边摊开的:
解锁的节目一多、围到七个人,最外面那两个正好站进芦苇后面。

**「时不时被挡住」比「一直被挡住」难查得多** —— 它只在节目解锁到一定数量
之后才出现,平时截图根本撞不见。

修法不是把芦苇挪走(那是纵深的来源),是让人躲开它:算位置时超过
`CROWD_RIGHT` 就翻到左边去。画面右边本来就窄,左边还宽着。

### 甲板分三条道

原来所有人都踩在同一条基线上,路人经过猫的时候两个身子重在一起,
分不出谁在前谁在后。甲板一共 26 格深,分成三层:

    -22  木棚,贴着栏杆
    -15  猫,晴天摊在棚门口
     -4  路人走的道,压在甲板最前沿

**间距要拉到 11 格才够。** 人有 24 格高,间距太小的话即使前后关系对了,
看着还是糊在一起。画的顺序跟着这三层走:棚 → 猫 → 路人。

### 甲板上谁压谁:一条规则

**谁靠前谁后画。** 听着是废话,但它有两个容易漏的地方:

一是**跨函数**。路人原来画在围观之前,而路人走的道(-4)比围观(-13)更靠前 ——
后排的人于是盖住了前排走过的人,一排人的前后关系整个反过来。
现在顺序是:棚(-22)→ 猫(-15)→ 围观(-13)→ 路人(-4)→ 近景芦苇。

二是**同一批人内部**。七个围观的原来踩在同一条线上,是一堵墙不是一圈人;
给每人错开 0~6 格之后,**必须按错开量重新排序再画**,不然就在小范围里
重犯上面那个错。

自动化验法:临时替掉 `CanvasRenderingContext2D.prototype.drawImage`,
把每一笔精灵图落地的基线记下来,断言它是递增的。
(**别用 Proxy 包 ctx** —— 内部方法会丢掉 this,直接 Illegal invocation。)

### 折耳根的睡姿返工了四次

第一版身子是一根从头到尾一样粗的长椭圆,尾巴贴着底边平铺成一道细杠 ——
整个下半截读成一条毛毛虫,尾巴根本看不出是尾巴。
第二版加了个屁股包,但两个圆挨着画完被 `outline()` 焊成了一整块,
轮廓还是一根管子。

第三版背拱得太高、尾梢挑到肩膀那么高 —— 睡熟的猫是塌下去的,不是弓起来的;
第四版把背压平之后又平过头,而且耳朵改成宽矮的,读成了兔子。

第六版脑袋描一圈、每只耳朵各描一圈,耳根那儿多出一道线 ——
**看着像一颗人头上扣了两只猫耳**。

贯穿这六版的其实是同一条规律:

> **描边要按「一个东西」来描,不是按「一个图形」来描。**
> 脑袋加耳朵是一个东西,里头不该有线;
> 脑袋和身子是两个东西,中间必须有线。

代码上就是:脑袋和耳朵在一张小图上画完、**整体** outline 一次,
再盖到身子上(`head_with_ears()` + `stamp()`)。

最后立住的做法:

* **换姿势**:从「摊平的一条」改成「团起来的一个球」。
  猫睡着的招牌轮廓是圆的,不是长的。
* **脸是钻石形,不是圆的 —— 但收口要带弧度。** 这一条最要命,也是最后才
  找到的:用椭圆当脑袋,下巴一圆就成了人脸,而且**耳朵怎么改都救不回来**,
  尖耳、圆耳、大耳、小耳前后试了四轮,问题一直在下巴上。
  可换成钻石形又走过了头 —— 从最宽处匀速收到四格宽,收出来是个尖楔子,
  那是老鼠的吻部。**猫脸的正确写法**:脑门宽而平(耳朵之间那一片),
  颧骨最宽处连着三行不收,之后先每行少 1、再每行少 2,收到七格宽的
  小下巴就打住。下巴要小,但不能收成一个点。整体是扁的,宽比高多。
* **脑袋探出身子左边**:缩在身子里的话它和背连成一片,耳朵也没地方立。
  一只看不见耳朵的猫,是一坨面包。
* **耳朵要小、要矮、要往两边靠**。折耳猫的耳朵本来就是往前扣的一小片;
  又高又大的一对立在脑门正上方,那是老鼠。露出脑袋轮廓两三格就够 ——
  露多了开始抢脸的戏份。里头压一档暗色,不然只是两个橙疙瘩。
* **尾巴自带一道边**:先铺一圈 `K` 再填色。只靠整体 `outline()` 的话
  尾巴和身子是连着的,等于没画。尾梢**不往上翘** —— 翘起来那一截在这个
  尺度上读成一片鱼鳍。
* **不画嘴**:这个尺度上嘴只有两三格,画出来是脸上多了几个黑点,不是一张嘴。
  闭着的眼加一个粉鼻头就够了。

---

## 六之二、记下来:西米露的信(不着急做)

一个还没做的功能,但方向已经想清楚了,记在这儿免得忘。

### 它要解决什么

后期鸥币有富余。第一反应是「加一个无底洞」,但那是**设计的无底洞** ——
升级线越长,数值越难验,`UPGRADES` 那张表迟早没人敢碰。

寄信是一个**终点型的钱坑**:钱花出去就离开经济系统了,不再变成新的产出,
所以它永远不需要再平衡。这是它比加档位好的根本理由,不只是好玩。

### 是什么

攒够一笔钱,哇鸥的好朋友**西米露**(一只爱旅游的小乌龟)会替你把一封信
带给另一个玩家。本质是玩家之间的异步信件交换。

### 关键的一次纠正:玩家不设计信

第一版方案是「玩家用现有素材拼一张明信片」。**这条走不通**,而且原因不是
素材还不够多 —— 是位置放错了:

**旅行青蛙里玩家也不设计明信片。** 青蛙自己出门、自己拍照、自己寄回来。
玩家只做三件事:收拾行李、等、拆信。它的情感价值不来自「这张图是我做的」,
来自「这是我那只蛙去的地方」。**是记录,不是创作。**

所以正确的做法是:**信由哇鸥自己写,内容从存档里长出来** ——
今日签、戴着什么、图鉴见过几样、去过哪些地方、这周撞见的事(直接用事件日志
的原话)、摊子卖了多少、棋局战绩、季节天气。玩家只勾几条要带上的,
再从句库里挑一句结尾。

**这条路要的新美术量是 0。**

而收信那头才是爽点:拆开看见的是另一个真人这周的滇池,而且因为玩的是同一个
游戏,细节全读得懂 ——「他居然集齐图鉴了」「他也抽到大凶·见手青」。
**这是只有玩家之间才认得的暗号。**

这是个赌注,赌的是「玩家对自己那份存档有感情」。旅行青蛙赌的也是这个。

### 如果要玩家的手笔,那个位置在「寄哪一张」

不是「拼哪几块」。从**你真收集到的**明信片里挑一张附上 —— 在 10 个去过的
地方里挑一个寄给陌生人是有分量的选择,在 4 个背景里挑一个不是。

前提是那 10 张明信片得**真的画出来**(现在是名字 + 借来的图标)。

### 三条必须守住的技术前提

**一、内容是数据,不是图片。** 附带的东西存成
`{bg:2, landmark:5, wear:['douli']}` 这种几十字节的结构,收信方**用自己那份游戏的素材现场画**。这一个决定同时解掉:图片审核(白名单天然合规)、对象存储、画风失控。自由文本和自由绘画留到以后,那是另一个量级的工程。

**二、匿名 ID,不做账号。** 每台设备生成一个 id,存 localStorage
**并塞进存档码**(存档码是整个 state 的 deflate+base64,加个字段就自动跟着走,导入存档 = 找回信箱)。

**三、收信人由西米露随机决定,玩家不能指定。** 不需要好友列表、搜索、加好友;也不存在定向骚扰。设定上完全说得通 —— 乌龟走到哪儿算哪儿,本来就是漂流瓶。
只允许对收到的那封信回一次,一跳,不建通讯录。

### 技术栈(两条路,看最后发在哪)

- **微信/抖音小游戏**:平台云开发自带云函数 + 数据库 + 内容安全接口,
  平台侧承担一部分合规,顺带解决变现。代价是不再是一个能随手发链接的网页。
- **Cloudflare Workers + D1**:免费额度绰绰有余、零运维。三张表
  (`players` / `letters` / `blocks`)、四个接口(寄信 / 看信箱 / 拆信 / 举报),
  **不需要对象存储**。

按上面三条前提做,后端大概一个周末。**一旦放开自由文本或自由绘画,
它会变成一个几个月的合规工程。** 这个分界线非常陡。

### 还没想好的

- **门槛定多少。** 按 `sim.mjs`,休闲玩家第 28 天才买满升级线,之后才谈得上
  攒钱寄信 —— 这个功能头一个月是隐形的,而社交功能最需要人的时候恰恰是最早期。
  倾向:第一封便宜,之后阶梯涨。
- **冷启动。** 信箱永远不能空:官方信(哇鸥写的、西米露旅途寄回的、
  运营方发的)混在里面兜底。这既解决冷启动,又给账号一个**在游戏里连载**的位置。
  真没有别人时,西米露就带一张它自己旅途上的明信片回来。
- **延迟是特性。** 乌龟送信走一到三天,正好把「其实没几个人在线」藏起来。

### 一件要甲方定的事

**西米露是一个新角色**,按分成协议第二条,角色形象著作权归甲方。
形象和性格该由他定,不该我们这边先画出来。

### 分期

1. 先把 **10 张明信片真画出来**(它本身就该做,见下)
2. 有真实日活之后再谈服务器 —— 到那时才判断得了值不值得开

---

## 六之三、码头美术(第一轮)

todo 第一条的前三项。**第四项「表演动作精细化」没做** —— 那要改
`WAOU` / `waou_wing` / `waou_bow`,而哇鸥本体的素材现在是冻结的
(甲方在照着做参考动画)。解冻之前不碰。

### 纵深

原来画面只有三层:天、水、脚下的栈桥。中间是空的,所以看着像三条平铺的带子。
补了三层:

- **中景**:大坝往右后方延伸的一段,配上一排越来越矮越来越密的灯柱。
  透视里最直接的一条线索就是「同样的东西越远越小」,一排灯柱比什么都管用。
- **甲板加厚到 26 格,后侧加一道镂空的水泥矮栏。** 加厚是为了能站两排 ——
  原来所有东西挤在一条线上,画面必然是平的。矮栏做成镂空而不是实心挡板:
  实心的会把水面从中间切断,画面反而更闷;柱子之间透出水,才有「隔着栏杆看湖」
  的感觉。栏杆用一组单独的**石色**,满画面木色里插一道冷灰,层次立刻分得开。
- **近景**:压在最前的芦苇,故意画到出画。一张画只要最前面有一样东西被裁掉
  一部分,眼睛立刻就认定「我在这个空间里面」。近景不需要细节,需要的是遮挡。

后排陈设走**空气透视**(往天空色混一档),光是错开 y 还不够 ——
人眼判断远近靠的是对比度,不是位置。

顺带修了个真 bug:`farSprite()` 的精灵图缓存键只用了 `name`,
同一张图既画近的又画远的时,后调的那次会直接拿到前一次的缓存,远近长得一模一样。
现在键里带上了远近、天气和时段。

### 游客(两版)

**第一版画的是背影**,理由是不用画脸、也不怕和哇鸥的豆豆眼打架。
**这版被否了** —— 背影的代价是这帮人没有表情、没有朝向,永远像一排背对着你的
模特。画风打架是个真问题,但解法不是回避,是把脸画得克制。

现在是 13×24 的正面:头发、两只眼睛、一道嘴。这个尺寸上脸只有五格宽,
所以五官**只保留眼睛和嘴** —— 鼻子、耳朵、瞳孔高光缩进画面就是一团脏点。

四个人靠头发色 + 衣服色 + 肤色 + 身高区分。肤色也分两档:一排人全是同一张
脸色最假。

每人三种姿势:

- `stand` 站着看
- `wave` 举起一只手。**手要举到脑袋那么高才看得出来** ——
  第一版只在肩膀上点了一格肤色,缩小之后完全看不见
- `walk` 迈开腿,两条腿一前一后。第一版是向两边叉开,那读成「站桩」不是「在走」

### 行为

围观的人从最多 5 个加到 7 个,而且**每人有自己的举手节奏**;
真有人投喂的那一下(`fedNow`)会多一个人举手 —— **动作和规则层的产出是同一件事,
不是各演各的**。一排人整齐地一起动是最假的。

另外加了一批**只是路过的人**(`drawStrollers`),和围观的分开:围观是玩法的一部分
(节目越多人越多),路过纯粹是「这地方有人气」—— 一个大坝上不可能所有人都在
看一只鸟。走路就两帧,站姿和迈步来回换;三帧以上在这个尺寸上看不出区别。

最近的一对围观者离哇鸥 28 格。人是 24 格高、哇鸥只有 16 格,围太近的话
主角直接淹没在一排人腿里。

### 影子和路灯

**影子是纵深里最便宜也最有效的一笔** —— 一样东西没有影子就像贴在背景上的纸片。
摊子、草棚、缆桩、路灯、每个人、哇鸥,现在都有。太阳画在右上,所以影子往左下拉;
用半透明的黑而不是调色板里的暗色,这样压在木纹、石头、任何底色上都成立。

路灯和栏杆原来糊成一团。第一反应是挪位置,挪完发现**那不是位置问题是高度问题**:
灯杆只有 26 格高,灯头正好落在栏杆横杆上(栏杆顶在甲板上方 39 格),
挪到哪儿都一样。把灯杆加长到 44 格,灯头高过栏杆,一下就清楚了。
x 也躲开了栏杆立柱的间距,免得杆子和柱子重成一根。

### 天色统一

顺带修了一个一直都在的毛病:摊子、缆桩、路人、围观的人、哇鸥自己
**都没跟着天色走**。白天看不出来,傍晚和夜里整个大坝暗下去,只有这几样
还是白天那么亮,像贴上去的。

为此把 `PHASE` / `shade` / `shadedSprite` 抽成了 `js/game/tint.js` ——
`wear.js` 也要用它,而 `scene.js` 本来就 import `wear.js`,反过来再 import 会成环。

**这是画的时候上的色,素材本身一格没动。**

### 摊位随升级改样貌(三版)

**升级要看得见。** 四条线原来只是数字变大,玩家花掉几十万鸥币,
画面上一格都没变,那笔钱花得毫无实感。

**第一版**只在 3 级和 6/7 级各变一次。看着够了,一算账才发现远远不够:

| | 1–6 级一共花 | 7 级以后花 | 花在没有画面变化那段 |
|---|---|---|---|
| 炉子 | 9,726 | 943,909 | **99%** |
| 招牌 | 14,589 | 1,415,864 | **99%** |
| 货架 | 24,314 | 357,056 | 94% |
| 保温箱 | 32,419 | 476,074 | 94% |

**第二版**把每条线拆成四五档,该变的地方都变了。但**做法还是错的**:
摊子本体固定不动,升级就在旁边多摆一个箱子、多摆一个灶 ——
堆到满级是**一排叠叠乐**,而且「多一个箱子」根本不像「生意做大了」。

**第三版:换整张摊子。**

|  | 总级数 | 长什么样 |
|---|---|---|
| 1 路边摊 | 4–9 | 一张折叠桌,搭块布,一口锅。风一吹就得收摊的那种 |
| 2 支起棚子 | 10–19 | 两根柱子撑起条纹雨棚,有了正经柜台和菜牌 |
| 3 木屋铺面 | 20–31 | 有墙有屋顶了,开一扇卖饭的窗,挂上招牌,烟囱冒烟 |
| 4 街边专卖店 | 32–44 | 瓦顶带檐、玻璃橱窗、通亮的灯箱招牌 |

**轮廓一段比一段大**,这才是「耳目一新」——玩家隔几天回来一看,
这不是同一个摊子了。

判定用**四条线的总级数**(4 到 44),不是某一条:铺面是整体投入的结果,
只升炉子不该让门面变成专卖店。

单条线的等级不再靠「旁边多一个箱子」表示,而是**长进这栋房子里**:

- 炉子高 → 烟冒得更凶(前两段从锅上冒,后两段从烟囱冒)
- 招牌高 → 前两段挂块牌子,三段起夜里灯箱亮起来,满级挂一串彩旗
- 前两段摊子小,旁边还摆得下灶和箱子;**三段起东西都收进店里了**,
  再往旁边堆就又回到叠叠乐

信息量一样多,但看着是一家店在变好,不是一堆杂物在长高。

四张图是**拼出来的不是手写的**(`tools/stall.py` 里的 `rect`/`frame`/
`awning`/`tiles`/`planks`)。最大那张 76 格宽 —— 手写一行数七十多个格子,
数错一个整张歪掉,而且以后想把窗户挪两格得重数一遍。

每段自带锚点(烟从哪冒、招牌灯箱在哪),写在 `STALL_STAGES` 里:
四张图大小不一样,把偏移写死在画的地方,换一段图就得满文件找。

另外两件:

- **蒸汽是唯一会动的那一项。** 别的都是静态的多一样东西,只有它在说
  「这摊子正开着火」。所以不烤进静态图层,每帧现画。
- **买下去的那一瞬间冒一串金色小十字。** 静态的是「结果」,这个是「事件」——
  摊子页和大坝画面同屏,买的人一定看得见。判定放在 `Background` 里自己盯着
  总级数变没变,不从 UI 层传「买了」进来:不管从哪儿买的都算,漏不掉。

旧的固定摊子 `STAND`、以及第二版才加的 `sign_lit` / `rack` 都删了 ——
换了做法之后没人用,留着只会让下一个人以为它们还在生效。

### 游客(两版)

**第一版画的是背影**,理由是不用画脸、也不怕和哇鸥的豆豆眼打架。
**这版被否了** —— 背影的代价是这帮人没有表情、没有朝向,永远像一排背对着你的
模特。画风打架是个真问题,但解法不是回避,是把脸画得克制。

现在是 13×24 的正面:头发、两只眼睛、一道嘴。这个尺寸上脸只有五格宽,
所以五官**只保留眼睛和嘴** —— 鼻子、耳朵、瞳孔高光缩进画面就是一团脏点。

四个人靠头发色 + 衣服色 + 肤色 + 身高区分。肤色也分两档:一排人全是同一张
脸色最假。

每人三种姿势:

- `stand` 站着看
- `wave` 举起一只手。**手要举到脑袋那么高才看得出来** ——
  第一版只在肩膀上点了一格肤色,缩小之后完全看不见
- `walk` 迈开腿,两条腿一前一后。第一版是向两边叉开,那读成「站桩」不是「在走」

### 行为

围观的人从最多 5 个加到 7 个,而且**每人有自己的举手节奏**;
真有人投喂的那一下(`fedNow`)会多一个人举手 —— **动作和规则层的产出是同一件事,
不是各演各的**。一排人整齐地一起动是最假的。

另外加了一批**只是路过的人**(`drawStrollers`),和围观的分开:围观是玩法的一部分
(节目越多人越多),路过纯粹是「这地方有人气」—— 一个大坝上不可能所有人都在
看一只鸟。走路就两帧,站姿和迈步来回换;三帧以上在这个尺寸上看不出区别。

最近的一对围观者离哇鸥 28 格。人是 24 格高、哇鸥只有 16 格,围太近的话
主角直接淹没在一排人腿里。

### 影子和路灯

**影子是纵深里最便宜也最有效的一笔** —— 一样东西没有影子就像贴在背景上的纸片。
摊子、草棚、缆桩、路灯、每个人、哇鸥,现在都有。太阳画在右上,所以影子往左下拉;
用半透明的黑而不是调色板里的暗色,这样压在木纹、石头、任何底色上都成立。

路灯和栏杆原来糊成一团。第一反应是挪位置,挪完发现**那不是位置问题是高度问题**:
灯杆只有 26 格高,灯头正好落在栏杆横杆上(栏杆顶在甲板上方 39 格),
挪到哪儿都一样。把灯杆加长到 44 格,灯头高过栏杆,一下就清楚了。
x 也躲开了栏杆立柱的间距,免得杆子和柱子重成一根。

### 天色统一

顺带修了一个一直都在的毛病:摊子、缆桩、路人、围观的人、哇鸥自己
**都没跟着天色走**。白天看不出来,傍晚和夜里整个大坝暗下去,只有这几样
还是白天那么亮,像贴上去的。

为此把 `PHASE` / `shade` / `shadedSprite` 抽成了 `js/game/tint.js` ——
`wear.js` 也要用它,而 `scene.js` 本来就 import `wear.js`,反过来再 import 会成环。

**这是画的时候上的色,素材本身一格没动。**

### 摊位随升级改样貌(两版)

**升级要看得见。** 四条线原来只是数字变大,玩家花掉几十万鸥币,
画面上一格都没变,那笔钱花得毫无实感。

第一版只在 3 级和 6/7 级各变一次。看着够了,**一算账才发现远远不够**:

| | 1–6 级一共花 | 7 级以后花 | 花在没有画面变化那段的比例 |
|---|---|---|---|
| 炉子 | 9,726 | 943,909 | **99%** |
| 招牌 | 14,589 | 1,415,864 | **99%** |
| 货架 | 24,314 | 357,056 | 94% |
| 保温箱 | 32,419 | 476,074 | 94% |

成本曲线是 2.5 的公比,后面几级贵得离谱,而那几级恰恰一点反馈都没有。
**这个数是写完第一版之后顺手算的,不算不会发现** —— 光看画面,
3 级和 7 级确实变了两次,像是做到位了。

第二版每条线在 1 到满级之间分四到五档:

| 线 | 3 级 | 5–6 级 | 7–8 级 | 9–11 级 | 12 级 |
|---|---|---|---|---|---|
| 炉子 | 小灶台 | 灶口透火 | — | 双灶 | + 冒蒸汽 |
| 招牌 | 小木牌 | 换成大木牌 | — | 带灯泡,夜里亮 | + 挂一串彩旗 |
| 货架 | 一个箱 | 两个叠起来 | 三个 | 换成一整排货架 | — |
| 保温箱 | 一个 | — | 两个 | + 盖上一块布 | — |

新素材只加了三样:`sign_s`(小木牌)、`rack`(三层货架)、`flags`(彩旗)。
双灶和多个保温箱直接复用同一张图摆两次 —— 「同一样东西多了一个」本身就是
读得出来的信息,不用为它再画一张。

三件配套的事:

- **蒸汽是唯一会动的那一项。** 别的都是「多一件东西」,只有它在说
  「这摊子正在开火」。所以它不烤进静态图层,每帧现画。
- **买下去的那一瞬间冒一串金色小十字。** 静态的多一件东西是「结果」,
  这个是「事件」—— 摊子页和大坝画面是同屏的(画面在上、面板在下),
  买的人一定看得见。判定放在 `Background` 里自己盯着升级总级数变没变,
  不从 UI 层一路把「买了」传进来:这样不管从哪儿买的都算,漏不掉。
- **升级等级进了图层重烤的 key**,不然玩家买完升级,画面要等到天气轮换才跟上。

配件摆在摊子**旁边**而不是身上:贴在摊子上要精确对准它内部的柜台和篷顶,
`stand` 那张图一改所有偏移全废;摆在旁边只依赖甲板那条线。

---

## 六之三·五、界面:按钮围着画面排

原来是「画面在上,一长条面板在下」。面板一长,玩家往下划的时候码头就出画了 ——
**而这游戏的主角是那片湖。** 要升级、要做菜,都得先把风景划走。

中间试过一版折中:在大坝页顶上摆一格功能按钮。**那不对** ——
它还是「先进大坝页,再点进去」,面板照样在画面底下接着。

现在是**机身**:功能按钮贴着画面的左、右、下三边,点一个,内容以**抽屉**
的形式压在画面上,压不满。露出来的那一截就是「码头还在」。

    左边  大坝 / 出摊 / 摊子 / 小屋        —— 玩法
    右边  背包 / 图鉴 / 明信片 / 成就       —— 收集与状态
    下边  出发觅食(主按钮)/ 装扮 / 聊天 / 存档

**没有页签了。** 页签的语义是「切换整页」,而这儿画面从不切换,
只是在它上面开一个抽屉。按钮本身就是开关:再点一次正开着的那一页就收起来,
玩家不用去找关闭按钮。

### 尺寸上的两个坑

**一、`.wrap` 得加宽到 1060。** 之前是 916(正好一块画面)。
左右两条按钮各 62 宽、两道 10 的缝 = 144,全从画面身上抢 ——
canvas 被挤到 736,又掉出整数倍缩放。1060 = 916 + 144。
断点也跟着定在 1059:再窄一格,竖排就挪到画面下面横着摆。

**二、`aspect-ratio` 算的是 border-box。** 这是个一直都在的老 bug:
`.px-screen` 写的是 `aspect-ratio: 880 / 620`,但 `box-sizing` 是 border-box、
边框 4px —— 内容区实际是 880×**618**。整块画面竖着被压掉两行,
横向是整数倍、纵向不是,像素一行高一行矮。改成 `888 / 628` 之后
量出来正好 880×620。

**三、给 flex 元素加 `hidden` 得自己补一条 `[hidden]{display:none}`。**
`.px-drawer` 设了 `display:flex`,盖掉了浏览器默认的那条 ——
于是「收起」之后抽屉还在,只是空的。截图里一眼看见,但代码上完全无痕。

### 手机上

塞不下左右两条竖排,全挪到画面下面横着摆三行。抽屉改成从下面拉上来,
盖到 46%。**再往上盖就只剩天空了**,那算不上「看得见码头」;46% 这条线
正好还留着西山、湖面和远处那段堤。手机上做不到既看见栈桥又留出能用的抽屉,
只能取「一眼认得出是滇池」。

---

## 六之四、图标重画

九道菜原来全在借食材的图标:鲜花和鲜花饼一模一样,菌子一个图标背着见手青和汽锅鸡。
**图标撞了,背包和摊子就等于没有信息** —— 玩家看见一朵花,不知道是原料还是成品。

现在九道菜各有各的图标,顺带重画了五样认不出的食材:

| 食材 | 原来 | 现在 |
|---|---|---|
| 饵块 | 带斑点的白椭圆,像块面包 | 压平的一块白米团 |
| 米 | 一个布袋 | 一堆米,颗粒感画进堆里 |
| 红糖 | 棕色方块,像块木头 | 压出来的糖砖,表面是糖粒 |
| 豆花 | 一个碗 | 隆起的一坨(碗留给豆花米线) |
| 乳扇 | 签子上的黄卷 | 摊平的一张奶皮(签子留给烤乳扇) |

后两条是**为了让料理认得出而改的原料** —— 豆花和豆花米线都画成碗、
乳扇和烤乳扇都画成签子上的黄卷,那和原来撞图没区别。
一对图标要分开,改哪一边都行,该改的是更容易画得独特的那一边。

四条升级线也全在借图标:炉子借辣椒、招牌借店铺、货架借背包、保温箱借红糖。
**保温箱那个尤其糟** —— 红糖重画成糖砖之后,升级面板上的「保温箱」变成了一块糖。
现在四条线各有各的:砖砌灶台(炉口透火)、吊起来的木牌(两根吊绳是它和明信片的区别)、
三层货架(横板是它和背包的区别)、带提手会冒热气的保温箱
(光画个箱子说不出「保温」)。

辣椒也重画了。原来那根是直的、上粗下细、橙红,读起来是胡萝卜 ——
**辣椒和胡萝卜的区别不在颜色在形状**:辣椒是弯的,蒂是一个带小帽的绿把,
不是一丛缨子。

挑形状不挑颜色:**16px 上颜色能分辨的档次很少,形状才是辨识度的来源。**
所以汽锅鸡画的是那根蒸汽烟囱(天底下只有汽锅有)、小锅米线画的是长锅柄、
烧饵块画的是卷口露出来的馅。

顺带四次被 `tools/emit.py` 的居中体检拦下(米、豆花、见手青、招牌各差一格)。
那个断言是当初为了图标高低不齐加的,这次实打实省了三轮返工。

---

## 七、暂不做

- **联机 / 排行榜** —— 存档码玩家可读可改(见 `README.md`),榜单数据不可信
- **付费**
- **真实地标**:明信片是像素画的再创作,不用实景照片

---

## 八、待定

1. ~~「欧币」这个货币名要不要换?~~ **已定:改成「鸥币」。**
   跟「鸥」同字,不再和欧元混。存档字段还是 `coins`,所以这次改名没有迁移。
2. 季节按现实时间走,还是游戏内自有节奏?现实时间更有陪伴感,但夏天会冷清好几个月
3. ~~飞行玩法留几分?~~ **已定:一分不给。**
   飞行只是**拿食材**和**跟自己较劲**的地方 —— 结算给食材、经验、明信片、
   最远距离纪录,就是不给鸥币。给了的话它立刻变成一条刷钱路线,
   而摊子那一整套(定价、升级、招人)的意义正好建立在「钱难赚」上;
   一个三分钟能刷一轮的小游戏会把它整个架空。
   实现上本来就没给过钱,这条是把它**写死成设计,别哪天顺手加上去**。
