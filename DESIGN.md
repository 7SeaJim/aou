# 哇鸥 · 去大坝整点饵块 · 策划稿 v0.2

> 状态:**P0 – P4 全部完成,存档 v7。**
>
> 网页版见 `design.html`(`npm run dev` 后访问 `/design.html`)——
> 同一份内容的两个视图:这份便于 diff 和搜索,网页版便于读和给人看,
> 而且里面的哇鸥是直接 import `js/game/pixels.js` 画出来的,形象一改就跟着变。
> **改了一边记得同步另一边。**

## 一、一句话

在昆明滇池边,陪一只没跟着鸥群飞回西伯利亚的红嘴鸥,慢慢把一个小吃摊做起来。

---

## 二、设定

### 2.1 为什么是红嘴鸥 —— 这不是设定,这是昆明的真事

每年 11 月到次年 3 月,红嘴鸥从西伯利亚飞几千公里到昆明越冬,聚在翠湖和滇池海埂大坝。
1985 年它们第一次成群进城,昆明人喂海鸥从此成了一年一度的城市仪式。

**红嘴鸥的招牌就是红嘴和红脚** —— 和形象稿一模一样。所以哇鸥不需要额外解释自己是谁,
它就是每年冬天昆明人排队去喂的那种鸟。

这条设定顺手解决了三件事:

| 问题 | 设定给的答案 |
|---|---|
| 主角为什么在这儿 | 鸥群春天飞走了,它没走 |
| 为什么有客人 | 游客本来就是来喂海鸥的,顺便买点吃的 |
| 季节循环从哪来 | 冬天鸥群在(热闹),夏天鸥群走(冷清)——天然的养成节奏 |

### 2.2 哇鸥

- **名字**:来自叫声「哇——」。
- **身份**:留下来没走的那一只红嘴鸥。暂时居住在昆明。
- **性格**:贪吃、话密、怕冷、对递到面前的食物毫无抵抗力。
- **它在干什么**:在海埂大坝支了个小吃摊,卖给来喂海鸥的游客。
  ——被喂了这么多年,总得回请一次。

### 2.3 场景:滇池

主场景从「一个泛泛的海边码头」改成**滇池 · 海埂大坝**:

- **前景**:大坝步道 + 哇鸥的小吃摊 + 栏杆、缆桩、成堆的鸥粮袋
- **中景**:滇池水面、渔船、成群的红嘴鸥(冬季密、夏季稀)
- **远景**:**西山睡美人** —— 侧卧的女子轮廓,是滇池最好认的天际线;
  现在画面里那两座无名小岛应该换成它
- **天气**:昆明四季如春,雨季(6–10 月)雷阵雨说来就来,干季晴得发白。
  现在的 晴 / 雨 / 雾 三档保留,雾改成滇池早晨的水汽

> 已完成:`paintIsland()` 换成了 `paintXishan()` —— 睡美人存成一条
> 「(x, 高出水面多少)」的折线,中间线性插值,想让鼻子再挺一点就改一个数。
> 灯塔删掉了(滇池没有灯塔),换成大坝路灯;`SCENERY.stand` 重画成中式小吃摊。
> 飞行视角把山按 0.55 缩了一档 —— 不缩的话山脊会顶进食材的生成区,东西掉山上就看不清。

---

## 三、形象规格(已定稿)

| 项 | 值 |
|---|---|
| 飞行图 | 32×32,四帧(上→平→下→平),身体不动只换翅膀 |
| 图标 / 站姿 | 16×16,`px-icon--waou` |
| 源文件 | `tools/scenery.py` `WAOU` / `tools/icons.py` `ICONS['waou']` |

配色(在 `tools/pal.py` 里新增了两格):

| 字符 | 色值 | 用途 |
|---|---|---|
| `X` | `#e8384f` | 哇鸥红 —— 嘴、脚。角色的招牌色,别挪作他用 |
| `V` | `#dfe4e8` | 肚皮灰 |
| `w` | `#fffdf4` | 身体白(沿用浪花白) |
| `K` | `#4a3628` | 描边、眼睛、翅膀(沿用深褐,不用纯黑,和全站描边一致) |

**三个识别点,任何新动作、任何尺寸都不能改:**

1. **身体是圆的**(或椭圆)。不做写实的鸟脖子、不掐后颈 —— 圆是这个角色的底。
2. **豆豆眼**:一颗实心圆点 + 一点高光。
3. **三根睫毛**,外侧那根往外撇一格;**红嘴红脚**用哇鸥红。

> v0.3 改过一次:眼睛原本是「白」字形(黑块中间一道白横)。
> 放到待机界面的大特写上之后太凶,不萌,整个角色的气质就偏了 ——
> 换成豆豆眼。**所有尺寸一起换**(16px 图标 / 32px 飞行四帧 / 表演三帧 / 特写),
> 待机界面和游戏里长两个样是最要不得的。

后续要补的动作(按优先级):

1. `idle` 待机(轻微起伏,偶尔眨眼)—— 摊位界面常驻,放置玩法里是主视觉
2. `eat` 吃东西
3. `happy` 开心(收成好 / 升级)
4. `sleep` 打盹(离线回来时的画面)
5. `cold` 发抖(冬天 / 雨天)

---

## 四、内容替换

### 4.1 食材

已全部替换(旧的 薯条/汉堡/鱼/贝壳/海藻/蛋/面包 是通用西式套路):

| key | 名字 | 来源 | 备注 |
|---|---|---|---|
| `erkuai` | 饵块 | 觅食 | 昆明早点的地基 |
| `potato` | 洋芋 | 觅食 | 云南话就叫洋芋,不叫土豆 |
| `rice` | 米 | 觅食 | 米线、凉虾都从这来 |
| `douhua` | 豆花 | 觅食 | |
| `flower` | 鲜花 | 觅食 | 玫瑰,斗南来的 |
| `mushroom` | 菌子 | 稀有 | 雨季限定,见 4.2 的彩蛋 |
| `rusan` | 乳扇 | 稀有 | 严格说是大理的,但昆明满街在卖 |
| `chili` | 辣椒 | 常见 | |
| `sugar` | 红糖 | 常见 | |

### 4.2 料理

| 料理 | 材料 | 解锁 | 备注 |
|---|---|---|---|
| 烧饵块 | 饵块 ×2 + 辣椒 | Lv1 | 开局第一道,昆明人的早饭 |
| 洋芋粑粑 | 洋芋 ×3 | Lv1 | |
| 米凉虾 | 米 ×2 + 红糖 | Lv2 | 夏季卖得好,冬季卖不动 → 季节机制的第一个挂点 |
| 豆花米线 | 米 ×2 + 豆花 ×2 | Lv2 | |
| 小锅米线 | 米 ×3 + 辣椒 ×2 | Lv3 | |
| 鲜花饼 | 鲜花 ×3 + 饵块 | Lv3 | |
| 烤乳扇 | 乳扇 ×2 + 鲜花 | Lv4 | |
| 汽锅鸡 | 菌子 ×2 + …… | Lv5 | 压轴 |
| **见手青** | 菌子 ×3 | Lv4 | **高风险高回报**:小概率进入一段「看到小人」的特殊对话,当天收益翻倍或归零。做成整个游戏最有名的那个梗 |

> 折耳根做成分歧道具:客人有的加钱要,有的看到就走。

### 4.3 明信片(昆明地标)

已换成 10 张昆明地标,收集顺序从近到远。每张配了一句哇鸥的短评,界面上直接显示:

1. **海埂大坝** —— 起点,喂海鸥的地方
2. **西山龙门** —— 抬头就能看见的那座山
3. **翠湖** —— 城里的另一个鸥群据点
4. **金马碧鸡坊** —— 最好认的地标
5. **昆明老街** —— 文明街那一片
6. **云大会泽院** —— 银杏
7. **斗南花市** —— 鲜花饼的鲜花从这来
8. **篆新农贸市场** —— 食材图鉴的老家
9. **南屏街**
10. **石林** —— 远足,后期解锁

> 明信片是**主要的长线收集目标**,建议每张配一句哇鸥的短评,
> 比图片本身更值钱。

---

## 五、玩法:转向放置养成

### 5.1 当初的问题(已解决)

原来是「一局一局的飞行小游戏 + 手动做菜」,每日 5 次觅食。
关掉页面等于什么都不发生,进度 100% 由主动操作驱动。
这对一个「陪伴感」的小游戏是错的 —— 玩家想的是「今天哇鸥怎么样了」,
而不是「我要打满 5 局」。

### 5.2 目标形态

```
摊位自动出货 ──→ 离线时也在出 ──→ 回来收一波 ──→ 投进升级 ──→ 出得更快
     ▲                                                          │
     └──────────────────────────────────────────────────────────┘

飞行小游戏从「主玩法」降级为「加速器」:
想要稀有食材(菌子、乳扇)才去飞一趟,不是每天必须打卡
```

一句话:**主循环变成放置,飞行变成可选的爆发**。

### 5.3 目标形态下需要的系统

| 系统 | 说明 |
|---|---|
| **离线结算** | 回来时按离线时长补产出。设收益上限(建议 8 小时),避免挂机一周回来一次性通关 |
| **摊位产能** | 每种料理有「每分钟产出」,受灶台等级、招牌等级、天气、季节影响 |
| **升级树** | 灶(产速)、招牌(客单价)、货架(离线上限)、保温箱(离线衰减) |
| **伙计鸥** | 其他红嘴鸥来打工,每只带一条被动加成 + 一段对话。**养成线的主体** |
| **季节** | 冬天鸥群在:客流 ×1.5,伙计可招募;夏天:客流低,但雨季菌子多 |

### 5.4 表演:食材的被动来源

P2 做完之后暴露的问题:**摊位一直在吃食材,食材却只能手动囤。**
挂机回来常常是「摊子停了,因为没材料」——放置玩法的手感直接断掉。

补法是给哇鸥加一个被动产出:不出去觅食的时候,它在大坝上表演,路人看了会投喂。

| | |
|---|---|
| 节目单 | 8 个,由**等级 / 成就 / 明信片**三处分别解锁 |
| 节目越多 | 围观的人越多、投喂越勤(20s → 9.8s)、给的种类越杂(2 → 9 种) |
| 等级越高 | 一次给得越多(`1 + floor(level/4)`) |
| 天气 | 下雨大坝上没人,投喂减半 |

解锁条件**刻意分散在三处**:让「去过的地方多」这件事真的有回报,
而不只是图鉴上多一个格子。

**它故意补不满。** 全解锁大概能养住一个没升过级的格子;炉子一升、格子一开,
供给立刻跟不上,还是得出去飞。补满了的话飞行玩法就又没意义了 ——
这是整个三角的平衡点:

```
        ┌──── 表演:被动、慢,补不满 ────┐
        │                              ↓
   觅食一趟 ─────→ 背包 ─────→ 摊位出餐 ──→ 欧币 ──→ 升级
   (主动、量大)      ↑          吃食材            ↑
                    └────────────────────────────┘
```

配套改了觅食的产出:`haulPerPickup = 1 + floor((level-1)/3)`,
Lv1 一次捡 1 个、Lv10 捡 4 个。摊位吃得快之后,再一颗一颗捡就跟不上。

顺带修了个老 bug:明信片掉落里写死着 `[...Array(7).keys()]`,
换成 10 张昆明地标之后**后三张永远掉不出来**。改成从 `POSTCARDS` 取。

### 5.5 P2 做完之后的三条记录

**① 摊位吃背包里的食材。** 这是当初没写死的一个选择,现在定了:摊位出一份就扣一份
材料,扣不动就停着。不这么做的话觅食和挂机是两件不相干的事,飞行玩法立刻失去意义。

**② 在线和离线共用 `produce()`。** 只有倍率不同(在线 1,离线走保温箱的折扣)。
分成两份实现的话,迟早在某次改数值时算出不一样的结果。

**③ 挖出一个真 bug:`tickStalls` 把任意时间差都当成在线。**
笔记本合盖三小时、标签页被浏览器冻结再唤醒 —— 页面还开着,但下一次 tick
会一口气看到几个小时的时间差,按在线全额结算,离线上限和折扣全绕过去。
结果是**挂着标签页睡一觉比老老实实关掉页面赚得多**,方向完全反了。
现在超过一分钟就转交 `settleOffline()`。

另外两个防挂机的细节:材料断供时把计时压回一个周期以内(不压的话补上材料会
一次性补发几百份);多个格子抢同一批食材时按格子顺序先到先得(简单、可预期,
玩家能靠调整顺序决定谁优先)。

### 5.6 数值骨架(实际落地的)

- 出餐间隔:`24 秒 ÷ 炉子系数`,炉子 1→10 级是 ×1.00→×3.52
- 每份收入:`菜价 × 招牌系数 × 天气系数`,招牌 1→10 级是 ×1.00→×2.62
- 天气:晴 1.0 / 雨 0.8(大坝上没人)/ 雾 0.9
- 离线上限:货架 1→8 级是 2→9 小时
- 离线折扣:保温箱 1→8 级是 40%→75%
- 升级成本:`base × 1.5^(等级-1)`,四条线的 base 分别是 60 / 90 / 150 / 200
- 格子:Lv1 一格,Lv3 两格,Lv6 三格

原稿写的是 `1.15^等级`,实际用了 `1.5^(等级-1)` —— 1.15 太平,
十级下来总成本还不够挂机一小时的收入,升级就没有选择感了。

### 5.7 落到现有代码上动了什么

| 文件 | 改动 |
|---|---|
| `js/state.js` | 加 `lastSeen`(时间戳)、`stalls`、`upgrades`、`crew`;**`SAVE_VERSION` +1 并补一条迁移** |
| `js/game/rules.js` | 加 `settleOffline(state, now)`,和现有 `settleFlight` 一样返回事件数组 |
| `js/storage/savecode.js` | 字段变多,注意存档码长度(现在约 350 字符) |
| `js/ui.js` | 主界面从「码头」改成「摊位」:显示待收、产能、升级按钮 |
| `js/game/flight.js` | 保留。触发条件从「每日 5 次」改成消耗体力 / 有目标地去找稀有食材 |
| `js/data.js` | 食材、料理、明信片、对话全部重写 |

⚠️ **离线结算用的是客户端时钟,改系统时间就能刷。** 单机无所谓 ——
这一点和现在 `refreshDaily()` 的处境一样,`js/game/rules.js` 里已经写了同样的注释。
将来若要做排行榜,时间必须由服务端判定。

---

## 五之二、待机界面

进游戏前先看见一张图:**哇鸥站在长虫山的观景台上眺望昆明。**

**是侧视,不是水平示意图。** 深度顺序:城市 → 石栏杆 → 哇鸥 → 草木。
石栏杆走透视线(远端高而细、近端低而粗),哇鸥站在近端、在栏杆**前面**,
左下角有一丛压在镜头前的枝叶 —— 像摄影师从灌木后面偷拍到的。

从远到近:天(色带,白天/傍晚/夜晚每次随机)、远山、
城市(**万达双塔**明显高出一截,是一眼认出昆明的那个点)、
栏杆、哇鸥(贴着右缘,只看得见左半边,朝左凝视)、草木。

几条做的时候才发现的:

- **中景不能空着。** 城市脚下到崖顶那一段一开始整块没画,直接漏出了
  `.px-screen` 的 CSS 底色,一片浅青,看着像水其实是个洞。得先铺满再往上画。
- **标题和按钮不能都摆正中**,那样正好压住双塔 —— 整张图的地标。
  改成标题放天上、按钮放城市下面那条带上。
- **特写就得够大。** 第一版哇鸥画了 60×84,摆在 440×310 的画面右下角只有巴掌大,
  不像特写像贴纸。放大到 100×132 才立得住。
- **草木不能缩在角落。** 第一版画成一小丛,读着是「草地上有点草」,
  不是「镜头前有枝叶」。要大到压过栏杆、尖子探进画面中段才有那个意思。
  叶子也得用真正的叶形(两头尖中间宽),圆点堆出来是一团黑饼。
- 游戏本体在待机界面背后已经跑起来了,点下去是立刻开始;
  离线结算的吐司先攒着,等玩家真进来了再弹,不然它会在标题画面背后自己弹完。

调试用 `?time=night` 钉住时段。

## 五之三、小屋

堤岸边的草棚,**全游戏唯一的近景**。别处都是远远看着哇鸥,只有这里能看清它的脸 ——
所以棚里除了必要的结构什么都不放,视线全给它。

**全游戏只有一个时钟。** 大坝的天光和小屋开不开门都从 `hourSlot` / `dayPhase` 读 ——
一开始小屋自己一套时段、大坝根本没有时间概念,两边对不上,这是个必须先修的地基问题。

| 时段 | 哇鸥在哪 |
|---|---|
| 11:30 – 13:30 | 小屋(醒着) |
| 19:00 – 22:30 | 小屋(醒着) |
| 22:30 – 05:30 | 小屋(睡着) |
| 其余 | 大坝上 |

「太晚了只能看它睡觉」这条是刻意的 —— 有个见不到的时间段,见到的时候才算数。

同步带来的两条连锁反应,都得跟着做,不然就是「看起来同步了」:

- **它不在大坝时,大坝画面里没有它,表演也不产食材。** 离线结算按五分钟一格
  采样算出那段时间有多少在大坝上(`damMsIn`)。睡着还有人投喂就说不通了。
- **入口做进背景,不是又加一个按钮。** 大坝上画了个草棚,点它进小屋;
  画和点共用 `SHACK_HIT` 一份坐标。

`hourSlot` 和 `dayPhase` **刻意分开**:前者管「哇鸥在哪」,后者管「画面什么颜色」——
它 11:30 回屋,但天不会在 11:30 变暗。

| 互动 | 说明 |
|---|---|
| **占卜** | 天气 + 贝壳花纹 → 八分之一。**八种是硬上限**:再多就成了随机文案,少了又转不出新鲜感。一天一次,让它成为一个仪式而不是一个按钮 |
| **海鸥四子棋** | 黑白石头是从大坝底下捡的。AI 会赢会堵、会避开送分,但**不做搜索树** —— 它是只海鸥,不是引擎 |
| **请它喝一杯** | 普洱茶 / 咖啡,每天上线随机给一杯。两样都是云南的(保山咖啡),不是随便挑的饮料 |

几条做的时候的判断:

- **占卜暂时只有文案,没有数值加成。** 给它挂个「今日产出 ×1.3」很容易,
  但那会让占卜变成每日必点的最优解,坏了「仪式」的性质。先留成纯风味,
  真要挂效果,得连着「不好的卦要不要有负面」一起想。
- **哇鸥在屋里尽量圆。** 第一版把眼睛按「大图就该多细节」画小了,出来是个蛋。
  比例得照 32px 那版等比放大 —— **眼睛占身宽约四分之一**,这是萌的关键。
- **腿是画的,不是帧图。** 走路时腿的角度一直在变,做成精灵图要画一大堆,
  而且改一次走路速度就得重画。

## 五之四、养成

**季节按真实月份走。** 红嘴鸥 11 月到次年 3 月在昆明越冬是真事,不该变成一个
游戏内的抽象计时器。代价是夏天冷清好几个月 —— 这是故意的:冬天鸥群回来时
才有那个落差,一年到头一样热闹就没有季节了。

夏天不是纯粹的惩罚:雨季稀有食材翻倍(菌子),所以夏天该屯货、冬天该开摊。

**伙计鸥**六只,一只管一件事、不叠加同类效果,玩家一眼看得出该先招谁。
两道门槛都是从设定里长出来的,不是硬塞的数值墙:

- **好感度** —— 得先和哇鸥处熟了,它才肯把亲戚介绍给你
- **只有冬天招得到** —— 鸥群不在昆明的时候,没人可招

**食材图鉴**记的是累计见过多少,花掉了不减。所有加食材的路径都走 `gain()`,
拿背包反推会越玩越少。老档升 v6 时按背包开个底 —— 手上有的显然见过,
不开底的话老玩家升上来是一本空图鉴,看着像坏了。

## 五之五、长线(P4)

P3 做完之后暴露的问题:**所有系统都各自封闭,玩通一条线不会让另一条线变好玩。**
成就页更是空转 —— 七条全是飞行玩法的遗物,放置、表演、小屋、伙计一条都没有,
而且达成了只弹一个窗,什么都不给。

P4 三件事其实是**一件事的三段**:成就产出羽毛,羽毛买装扮,装扮的解锁条件
又挂回明信片 / 伙计 / 好感度。随机事件负责把这条线撒到日常里。

### 5.5.1 成就重做

七条扩到 28 条,按玩法分五组(觅食 / 摊子 / 大坝 / 小屋 / 见闻),
组内分三档。分组不是为了好看:**平铺一长条时玩家看不出「还有哪块没碰过」,
分组之后空着的那一栏本身就是引导。**

判定要靠计数器,不能拿背包和图鉴反推 —— 花掉的、送出去的都该算数。
所以存档里加了 `stats`:出去觅食几次、被投喂几次、摊子出了几份、
请喝了几杯、赢了几局棋、撞上几件事、累计挂机多久。

老档升 v7 时这些**只能从 0 起**。四子棋战绩和今日占卜本来就存着,
那两处开个底;其余补不出历史,与其瞎猜不如诚实归零 ——
老玩家会慢一点,但不会凭空多算。

还修了一个一直都在的漏子:成就原来只在「做菜 / 交订单 / 离线结算」时判,
**纯挂机涨上来的等级、钱、投喂次数一直不算数**。现在在线 tick 每秒也判一遍。
代价是老档进来会一口气达成一串,所以吐司超过三条就并成一条。

### 5.5.2 装扮

五件,两个槽位(头 / 脖子),一个槽只戴一件。

**用羽毛买,不用欧币。** 两条线抢一个钱包的话,玩家买了帽子就升不了炉子,
装扮会变成「明知道该忍住的浪费」。羽毛只从成就来,于是「打扮」的进度条
就是成就本身。全部达成共 120 根,五件总价 68 根 —— 留了余量,
不至于逼玩家把成就刷满才能买齐。

解锁条件全挂在别的玩法上:花环要去过斗南花市、头巾要招够三只伙计、
铜铃要好感度 30。**逼着玩家把三条线都碰一遍。**

素材在 `tools/wear.py`,每件两套图:

- `big` 给小屋近景那只 124×99,按 46 宽画完再 ×2 —— 直接画 92 宽一行要数
  九十多个格子,数错一个就歪;放大两倍之后描边正好 2px,和 hut_waou 一致
- `small` 给大坝上那只 16×16,十几个格子直接画

**一套图缩放到另一个尺度不是糊就是一堆大方块**,所以必须两套。
锚点按「头顶那一行」对齐而不是精灵图底边 —— 大坝那三帧高度不一样,
鞠躬那帧头低了 3 格,按底边对齐帽子会浮在脑袋上面。

飞行玩法里的 32×32 和待机界面的特写**暂时不戴** —— 那是第三、第四套图。

### 5.5.3 大坝随机事件

18 条,哇鸥站在大坝上的时候平均九分钟撞上一件。和表演共用同一条判据
(`damMsIn`)—— 它人在小屋里,坝上发生什么都跟它没关系。

三条约束,让事件是「调味」不是「主收入」:

1. 一次的量都不大,抵不上一趟觅食;量跟等级走,但只跟到能看出差别为止
2. **有好有坏。** 全是好事的话,事件就只是个慢速的自动奖励 ——
   小孩追鸥群会把表演进度清零,来巡的人会把摊位计时清零
3. 离线照常发生,但打折并且**一次最多补 6 件**。不封顶的话出门一天
   回来是三十条日志,一条都不会有人看;而且坏事攒一堆一起结算,
   玩家只会觉得被坑了

结算顺序是**事件 → 表演 → 摊位**,在线离线一致。反过来的话,
事件清掉的那些计时是「已经产出过一轮之后」才清的,等于凭空多给。

日志存的是**事件当时的原话,不是 id**:事件表以后会改,改完拿 id 反查
会让老日志对不上号。日志是「发生过什么」的记录,不是索引。只留最近 20 条 ——
存档码是要玩家复制的,日志无限长会把它撑成一大坨。

## 六、分期路线

| 期 | 内容 | 状态 |
|---|---|---|
| **P0** | 像素画基建、UI 组件库、哇鸥形象 | ✅ 已完成 |
| **P1 · 换皮** | 滇池场景、云南食材与料理、昆明明信片、对话重写 | ✅ 已完成(存档 v2) |
| **P2 · 放置内核** | 离线结算 + 摊位产能 + 升级树 | ✅ 已完成(存档 v3) |
| **P3 · 养成** | 表演玩法、待机界面、小屋、伙计鸥、季节循环、食材图鉴 | ✅ 已完成(存档 v6) |
| **P4 · 长线** | 成就重做、装扮、大坝随机事件 | ✅ 已完成(存档 v7) |

**P1 做完了,但当初那句「不碰存档结构」是错的。** 食材换名字就是在改存档:
`backpack` 的键从 `fries/burger/…` 变成 `erkuai/potato/…`,老档的背包会整个对不上。
实际做法是走了一次 `SAVE_VERSION 1 → 2` 的迁移,把旧食材按最近的对应关系换过来
(炸薯条→洋芋、汉堡和面包→饵块,两个旧键并进一个新键所以要累加),
明信片和订单清空重来(旧 id 对不上新地标,旧订单的 need 里是已经不存在的食材)。

顺带挖出一个真 bug:`migrations[0]` 用的是**当前**的 `FOOD_KEYS` 常量,
而它现在指向新键 —— v0 老档的背包会在第一步就被清零,第二步再也捞不回来。
**迁移函数只能依赖写死的、当时的字段表**,不能引用会随版本变化的常量。已修。

---

## 七、暂不做

- **联机 / 排行榜** —— 存档码玩家可读可改(见 `README.md`),榜单数据不可信
- **付费**
- **真实地标**:明信片是像素画的再创作,不用实景照片

---

## 八、待定

1. 「欧币」这个货币名要不要换?(和「鸥」谐音其实挺好,但容易和欧元混)
2. 季节按现实时间走,还是游戏内自有节奏?现实时间更有陪伴感,但夏天会冷清好几个月
3. 飞行玩法留几分?完全砍掉最省事,但它是目前唯一的「操作乐趣」
